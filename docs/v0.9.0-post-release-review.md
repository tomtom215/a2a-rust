<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- Copyright 2026 Tom F. <tomf@tomtomtech.net> (https://github.com/tomtom215) -->

# Post-v0.9.0 review

A critical read of this repository after the 0.9.0 release, written against
first-hand measurement rather than against the repository's own claims about
itself. Every number below was produced on this checkout during the review;
where something was *not* verified, it says so.

Two things frame it. The `a2a-protocol-types` crate has passed 20,000 downloads
(25,022 total / 24,268 in the last 90 days, crates.io API, 2026-08-17), so
mistakes here reach people. And since this project started, the A2A project has
published **its own Rust SDK** — `a2aproject/a2a-rs` — which changes what
"complete" has to mean.

## What was verified, and what it showed

| Check | Command | Result |
|---|---|---|
| Workspace build | `cargo build --workspace --all-features` | exit 0 |
| Workspace tests | `cargo test --workspace --all-features` | exit 0 — **2,837 passed, 0 failed, 157 ignored** |
| SLIMRPC binding build | `cargo build --all-targets` in `bindings/` | exit 0 |
| Vendored SLIMRPC spec vs upstream | `scripts/check_slimrpc_spec.sh` | exit 0 — 2 files match |
| Vendored protos | `scripts/check_proto_copies.sh` | exit 0 — 5 files × 4 copies identical |
| File-length ratchet | `scripts/check_file_lengths.sh` | 80 of 409 over 500 lines, all recorded |
| Book `ignore` ratchet | `scripts/check_book_code.sh` | 130 blocks across 21 files, matches baseline |
| API reference names | `scripts/check_api_reference.py` | 116 names, all defined |

The suite is real and it is green. The README said "~2,740 tests"; the measured
figure is 2,837 passing. Stale, and in the conservative direction — corrected in
this branch.

**Panic discipline is genuinely good, and is worth stating because it is the
thing most often wrong in a Rust server SDK.** Excluding `#[cfg(test)]` modules,
`build.rs`, benches, and doc comments, there are **52** `unwrap`/`expect`/`panic!`
sites across all four crates, and the runtime ones are lock-poisoning `expect`s
plus one `signal(SIGHUP)` registration. There are **zero** `TODO`/`FIXME`/`HACK`
markers in `crates/*/src` or `bindings/*/src`. This is not a weak area.

---

## 1. The thing that changed: there is now an official Rust SDK

`a2aproject/a2a-rs` publishes six crates. `a2a-lf` has **31,655** downloads
against this project's `a2a-protocol-types` at **25,022**. It is not a
side project — it is the A2A project's own.

| | this project | `a2aproject/a2a-rs` |
|---|---|---|
| Bindings | JSON-RPC, REST, **WebSocket**, gRPC, SLIMRPC | JSON-RPC, REST, gRPC, SLIMRPC |
| Server base | raw `hyper` 1.x, Axum optional | `axum` |
| MSRV | **1.93** | **1.85** |
| CLI | — | `a2acli` |
| Card-driven transport selection | four built-in bindings only | `TransportFactory`, SLIMRPC included |
| Conformance evidence | official TCK 92/114 MUST, in-repo runner, ITK, mutation ledger | none published |
| SLIMRPC multicast (`slimrpc-multicast.md`) | **implemented** | not implemented |
| SLIMRPC `Collaborate` (collaborative channel) | **not implemented** | implemented |

Read honestly, this project is ahead on transports, on testing, and on evidence,
and behind on three things that decide adoption:

* **MSRV 1.93 vs 1.85.** Eight minor versions of Rust. This is the single
  largest adoption barrier in the repository, and it is now measurable against a
  direct alternative rather than against a hypothetical conservative user. The
  ROADMAP lists it as an open question; the answer got easier.
* **No CLI.** `a2acli` inspects cards and manages tasks. Evaluating this SDK
  requires writing Rust; evaluating theirs does not.
* **No card-driven transport selection for SLIMRPC.** Theirs resolves a card and
  dials whatever it advertises. Ours requires `with_custom_transport` by hand,
  because `builder/transport_factory.rs` knows four binding constants and has no
  registration hook.

None of these is a defect. All three are the reasons someone picks the other one.

---

## 2. SLIMRPC: what "complete" actually requires

The binding implements all eleven methods plus multicast, tested across ten
topologies with **37 integration test functions** and real SPIRE. The official
crate's suite is **4 test functions in one topology**. On depth this is not
close.

Reading `spec/slimrpc_v1/slimrpc.md` clause by clause against the
implementation, and against the official crate for comparison:

### 2.1 Service parameters are not transmitted (§3, a MUST)

§3: *"A2A service parameters … **MUST** be transmitted using the SLIMRPC
metadata mechanism"*, with `a2a-version: 1.0` as its own example.

`SlimRpcTransport`'s `metadata_from` forwards `extra_headers` and nothing else,
and `ClientRequest::new()` starts that map empty. Every other client transport
in this workspace injects the version explicitly —
`transport/jsonrpc.rs:199`, `rest/request.rs:101`, `websocket.rs:255`,
`grpc.rs:235`. **SLIMRPC does not.**

The server half matches: `headers_from(ctx)` returns `ctx.metadata()` raw, and
nothing validates a version. Every other binding does — `validate_version_header`
for JSON-RPC and REST, `check_a2a_version` for WebSocket, `validated_metadata`
for gRPC.

Two things make this more than a missing line:

* **An out-of-workspace binding cannot fix the server half.**
  `validate_version_header` is `pub(crate)` and `validated_metadata` is
  `pub(super)`. This is the same shape of problem as the missing
  `EventStream::from_event_channel` that building this binding already
  uncovered: the extension point is incomplete, and reading the signatures does
  not show it.
* **The official crate does not send it either** — checked against
  `a2a-slimrpc` 0.2.4's source. So this is not an interop break between the two
  implementations. It is a §3 MUST that neither satisfies, and the honest
  framing is "unimplemented by both", not "we are behind".

### 2.2 Internal SLIMRPC metadata leaks into the A2A header map

The official crate filters SLIMRPC's own keys — `deadline`, `status-code`,
`rpc-id`, `service`, `method` — before handing metadata to the handler
(`common.rs::is_internal_metadata_key`). Ours does not: `headers_from` returns
`ctx.metadata()` unfiltered, and that map is what feeds `RequestHandler`'s auth
and tenant resolvers. A `HeaderTenantResolver` keyed on a colliding name would
read transport plumbing as an A2A header. Narrow, but it is a correctness gap
with a known-good reference fix.

### 2.3 Address parsing is stricter than the spec's own examples

`SlimName::parse` requires the `slim://` prefix and rejects anything else. But
§2.1 of the same spec presents bare `mydomain/demo/echo_agent` as *the* SLIM
Name form, and the official crate accepts all three of `org/ns/app`,
`slim://org/ns/app`, and `slimrpc://org/ns/app`. A card written by the official
SDK using the bare form is rejected by ours. One `strip_prefix` chain fixes it.

### 2.4 Two spec observations worth recording

* The vendored spec's §1 says the service is `a2a.v1.A2AService`; both
  implementations use `lf.a2a.v1.A2AService`, which is what the ratified
  `a2a.proto` package actually is. The spec text is internally inconsistent with
  its own §4. **Both implementations agree, so there is no interop break** — but
  the repository vendors that spec and says nothing about the discrepancy.
* `scripts/check_slimrpc_spec.sh` compares two named files. It cannot see a
  *new* file appearing upstream. The official crate references a "collaborative
  channel extension spec" that is not in `a2aproject/experimental-cpb-slimrpc`
  at all — so the mechanism by which that binding grows is one this repository's
  drift check is structurally unable to notice.

### 2.5 The binding is outside every supply-chain gate

`bindings/a2a-protocol-slimrpc` is `publish = true`, ships 379 transitive
dependencies including a native C crypto build, and:

* `cargo deny` runs at the workspace root, so **none of those 379 are audited**;
* the `package` job's `cargo package --workspace` cannot see it, so **nothing
  validates that it packages or publishes**;
* it is in no coverage or mutation run.

Its CI job runs fmt, clippy, build, test, and SPIRE. That is good, and it is not
the same as the four workspace crates' gates. For a crate that is published to
users, the audit gap is the one that matters.

---

## 3. Worst performance: persisted streaming events

Measured on this checkout with a probe written for the review
(`SqliteTaskStore`, fresh database per arm, three interleaved repetitions,
best-of-3 per bucket, release build):

| artifact parts | `save_artifact_delta` | full `save` | ratio |
|---:|---:|---:|---:|
| 100 | 170 µs | 203 µs | 0.84× |
| 300 | 287 µs | 295 µs | 0.97× |
| 500 | 359 µs | 426 µs | 0.84× |
| 1000 | 541 µs | 541 µs | **1.00×** |

Two findings, and the second is the uncomfortable one.

**Persisting one streaming event costs 170–540 µs.** The repository's own CI
benchmark agrees independently — `backpressure_append_volume/sqlite_store_502_events`
is 110.02 ms, or 219 µs/event — and that is against 2.83 ms for the same 502
events in memory, a **39× penalty**. At 200 µs/event, an agent emitting 100
events/second saturates a core at roughly 50 concurrent tasks. This is the
dominant cost of any persistent A2A agent built on this SDK, and SQLite is the
store the documentation recommends first.

**The 0.9.0 "incremental artifact persistence" does not deliver an asymptotic
win.** It is modestly better at small documents — which is where it was tuned,
and the code comments say so — and it converges to exactly the full rewrite by
1000 parts. The cause is visible in `artifact_delta_sql`: the statement is
`UPDATE tasks SET data = json_set(data, '$.artifacts[…].parts[#]', …)`, and
SQLite's `json_set` parses and re-serialises the entire `data` document. The
Rust-side serialisation is saved; the database-side rewrite is not, and it is
the larger half.

This is not a bug — the code does what it says. But "incremental persistence"
reads as an order-of-magnitude claim and the measurement does not support one.
A real fix means normalising parts out of the JSON blob into their own
append-only rows, which is a schema change, not a statement change.

**Second-worst, and less alarming:** `transport_jsonrpc_send/single_message` at
194.8 µs on loopback. That is a full HTTP round trip through hyper, and the
`realistic_interceptor_chain` series shows ten interceptors add only ~8 µs, so
the cost is the stack rather than this SDK's logic. It is not a defect; it is
the number to know before promising throughput.

---

## 4. Worst developer experience

Ranked by how many people hit it.

1. **MSRV 1.93.** Covered above. It is the first thing that stops an
   organisation from adopting, and it now has a 1.85 alternative.

2. **The Quick Start taught a server you could not ship** — fixed in this
   branch. `serve()` accepts forever and returns only if you drop its future,
   which cancels the accept loop and silently kills in-flight connections.
   `examples/deploy-agent` — the example whose entire subject is deployment —
   had to switch to the Axum adapter for `with_graceful_shutdown`. The path the
   README teaches and the path the production example uses were different
   functions, and nothing said so. `Server::bind(..).serve_with_shutdown(..)`
   now closes it.

3. **No CLI.** Evaluating this SDK means writing Rust and compiling. That is a
   real barrier at the "does this work at all" stage.

4. **Compile time.** A cold `--all-features` workspace build is minutes, and the
   test binaries take longer than the tests. Partly inherent to 16 feature
   combinations; still what a contributor feels first.

5. **Ten feature flags on the umbrella crate**, and the SLIMRPC binding is not
   among them — it cannot be, being out of workspace. `a2a-protocol-sdk` is sold
   as the batteries-included entry point and one binding is unreachable from it.

---

## 5. What misses the production-ready bar

The framing this repository already uses — *supported* and *verified* are
different claims — applied to the operational surface.

* **~~`serve()` cannot drain.~~ Fixed in this branch.**
* **~~`serve()` has no connection ceiling.~~ Fixed** for the new entry point.
  `serve()` and `serve_with_addr()` still spawn a task per accepted socket with
  no bound; the EMFILE backoff handles the symptom, not the cause.
* **No connection-level timeouts anywhere.** There is an `executor_timeout` for
  agent code and request timeouts on the client, but no header-read timeout and
  no idle timeout on the server. A slowloris connection holds a task
  indefinitely. This is the largest remaining hardening gap and it is not
  in the ROADMAP.
* **`serve()`/`serve_with_addr()` discard `serve_connection`'s result**
  (`let _ = …`). A connection that failed to negotiate and one that served a
  thousand requests are indistinguishable. Inconsistent with this project's own
  "make failure paths observable" work in 0.9.0. The new entry point traces it;
  the old two still do not.
* **`serve_with_addr` spawns and returns no handle** — the accept loop cannot be
  joined or aborted.
* **`panic = "abort"` in the release profile.** For binaries built from this
  workspace, any panic — including a poisoned-lock `expect` — takes down the
  whole process rather than one task. Defensible for a CLI, worth an explicit
  decision for a server, and currently it is a workspace default nobody records
  a reason for.
* **Rate limiting is per-process.** Three replicas mean 3× the configured limit.
  The module says so further down; the summary line said "token-bucket" when the
  implementation is a fixed window — corrected in this branch, because a fixed
  window admits 2× the limit across a boundary and anyone sizing against an
  upstream ceiling needs that from the summary.
* **No sustained-load or soak evidence anywhere in the repository.** Criterion
  measures latency at fixed concurrency for seconds. Nothing measures an hour, a
  leak, or a slow degradation. The SLIMRPC README is admirably explicit that it
  has none; the rest of the repository has none either and does not say so.

---

## 6. Documentation and GitHub Pages

The book is 11,593 lines across 43 pages, live at a2a-rust.com, with search,
themes, canonical URLs, and a sitemap. Structurally it is in good shape. What is
missing is thematic, and it maps almost exactly onto the headline features:

* **No chapter on multi-tenancy.** `TenantResolver`, `PerTenantConfig`, and four
  tenant-aware stores are a headline feature with no page of their own —
  scattered mentions across `stores.md` and `configuration.md`.
* **No chapter on observability.** Same story for `Metrics` and OpenTelemetry.
* **No chapter on security.** SSRF protection, body limits, CORS, path-traversal
  defence and header-injection prevention are spread across `production.md` and
  the API reference.
* **No troubleshooting or FAQ page at all.** Zero occurrences of either word in
  `book/src/`. For an SDK whose failure modes include "wrong `A2A-Version`
  header", "stream ended without a terminal state", and "SPIFFE handshake never
  completes", this is the highest-value missing page.
* **No upgrade guide.** A 0.x that says minor versions may break should tell
  people how to cross one. The CHANGELOG is 191k and is not that.
* **SLIMRPC is filed under "Building Agents"** rather than a bindings section,
  and its book page carries the only structural entry in the ignore-baseline.
* The examples README table lists six of eight examples — `hello-agent` and
  `deploy-agent` are absent from it.

---

## 7. Examples still to build

The six measured examples each hit 44/44 method×binding cells, and
`incident-response` runs sixteen production-hardening checks. The coverage
*within* the examples is not the gap. The gaps are whole categories:

* **No example uses SLIMRPC.** The binding is out of workspace, so `examples/`
  cannot depend on it — meaning the binding with the most novel operational
  surface (SPIFFE, multicast, fabric nodes) has zero presence in the place
  readers actually look.
* **`multi-lang-team` has no Rust worker.** Python, JS, Go, Java — and the ITK
  now has an obvious fifth language it does not cover: the official Rust SDK.
  Interop with `a2aproject/a2a-rs` is the single most valuable missing ITK leg,
  and it is missing because the SDK did not exist when the matrix was drawn.
* **No durability example.** Nothing demonstrates an agent restarting and
  resuming a long-running task from a persistent store. `incident-response`
  checks that a task survives a handler swap, which is close but is a check
  rather than a narrative.
* **No failure-injection example.** Nothing shows what an operator sees when the
  store is down, the push endpoint is refusing, or the queue is full. The
  `Metrics` hooks for exactly these were added in 0.9.0 and no example reads
  them under fault.
* **No horizontal-scaling example.** Two replicas behind a load balancer sharing
  a Postgres store is the first architecture anyone deploying will reach for,
  and it is where the per-process rate limiter and in-memory event queues stop
  being adequate. Nothing addresses it.

---

## 8. What misses the engineering-excellence bar

Little does, and it is worth being precise about which little.

* **`send_message_inner` is 526 lines** — measured, the largest function in the
  codebase, on the hottest path, carrying interceptors, extension and streaming
  checks, id and metadata validation, capacity admission, three-phase stale-token
  eviction, and task creation. Already recorded in `.file-length-baseline` with
  an honest note. It is still the one place where a reviewer cannot hold the
  whole behaviour in their head.
* **80 of 409 source files exceed the 500-line guideline.** Ratcheted, so it can
  only shrink — but a fifth of the tree is over the bar the project set.
* **`mutants.toml` was never read by cargo-mutants** (wrong path), so every
  published mutation score includes generated protobuf mutants and ran with a
  328 s budget rather than the 300 s cap the file claims. The ROADMAP documents
  this thoroughly and defers it. It is the one place where a headline number
  measures something other than what it says it measures.
* **Two doc defects found by reading**, both corrected here: the rate-limiter
  algorithm and two stale README figures. Both were in the first line a reader
  sees.

Against that: zero TODOs, 52 panic sites in non-test code across 140k lines,
`#![forbid(unsafe_code)]` everywhere, every gate proven able to fail by
injection, and a ROADMAP that documents its own measurement errors including one
that was wrong by an order of magnitude in the *optimistic* direction. That
combination is rare.

---

## 9. What I am least confident in

Stated plainly, because the rest of this document is assertive.

1. **Absolute performance numbers.** The SQLite figures were taken on a shared
   cloud VM with unknown I/O characteristics. The *relative* comparison
   (delta vs full save) is sound because the arms were interleaved on fresh
   databases; the absolute µs are indicative. That CI's independent 219 µs/event
   lands inside my 170–540 µs range is corroboration, not proof.
2. **The claim that `json_set` is the cause.** I read the generated SQL and the
   growth curve matches, but I did not profile SQLite itself. The mechanism is
   inferred from two consistent observations, not measured directly.
3. **The official SDK comparison.** Built from its published README, its
   `a2a-slimrpc` 0.2.4 source, and crates.io metadata. I did not build or run it.
   Feature claims from a README are not verified behaviour — the same standard
   this project applies to itself.
4. **Download counts as adoption.** 25,022 is real, but crates.io counts include
   CI, mirrors, and docs.rs builds. It is evidence of reach, not of production
   use, and the gap between those two is unknown.
5. **The 157 ignored tests.** I confirmed they are database-gated and that CI
   provides the service; I did not run them here.
6. **Anything about sustained behaviour.** No soak test was run, by me or by
   this repository, ever. Every statement about stability under hours of load is
   unevidenced — including the optimistic ones.

## 10. What fails the "proudly call it production ready" bar

The parts I would not defend without qualification, in order:

1. **Connection-level timeouts do not exist.** A server with no header-read or
   idle timeout is not one I would put on a public interface without a proxy in
   front, and the documentation does not say to.
2. **No soak evidence.** "Production ready" implies someone ran it for longer
   than a benchmark. Nobody has.
3. **Horizontal scaling is undemonstrated and partly unsupported.** The rate
   limiter is per-process and the event queues are in-memory. A second replica
   changes behaviour in ways nothing documents.
4. **The SLIMRPC binding is published without dependency auditing.** 379
   transitive dependencies, including native crypto, reaching users through a
   crate that `cargo deny` never sees.
5. **Persisted-event throughput.** 39× slower than memory, with the headline
   optimisation not delivering asymptotically. Fine for a control-plane agent,
   not fine for a high-rate streaming one, and nothing tells you which you have.

Everything else — the protocol surface, the conformance evidence, the testing
discipline, the honesty of the ROADMAP — I would defend as-is.

## 11. What I would do differently

* **Decide the MSRV against 1.85, not against a hypothetical.** It is now a
  competitive fact.
* **Ship a CLI.** It is the cheapest adoption unlock available and the official
  SDK has already established the expectation.
* **Bring SLIMRPC inside the gates that matter.** Not the workspace — that
  reasoning is sound — but `cargo deny` and `cargo package` should see it. A
  separate `deny.toml` in the binding directory and a `--dry-run` package step
  are both small.
* **Add the official Rust SDK to the ITK.** It is the highest-value interop leg
  available and the matrix already has the shape for it.
* **Stop optimising SQLite statements and change the schema**, or document the
  per-event cost as a hard number in the stores chapter so readers can size
  against it. The current position — an optimisation whose name promises more
  than it delivers, and no published per-event figure — is the worst of both.
* **Write the troubleshooting page before the next feature.** It is the largest
  gap per hour of work in the entire repository.

---

# Addendum: what this branch changed

Written after the review above, against the same standard — every claim below
was run on this checkout, and the corrections are corrections of *my own*
findings where measuring more carefully contradicted them.

## Two things I reported that were wrong, both in the optimistic direction

**The SQLite delta path was not converging to parity — it had become a
pessimisation.** The review said delta/save reached 1.00× at 1,000 parts. Run
through `SqliteTaskStore` itself rather than through a hand-written probe, it
crosses 1.0 and keeps going: **1.39× at 1,000 parts and 1.48× at 3,000**. Past
roughly 300 parts, the incremental path was slower than rewriting the whole
record, because it paid a `json_set` parse of the document on top of everything
a full save does. Appended parts now go to an append-only side table, and the
append is flat at ~130 µs from ten parts to three thousand.

**The header-read timeout was not missing — it was silently disabled.** The
review said there were no connection-level timeouts. Hyper *has* one and
defaults it to 30 seconds; it applies only when a `Timer` is installed on the
connection builder, and no server here installed one, so `Time::check` logged
`timeout \`header_read_timeout\` has default, but no timer set` and returned
`None`. Every component was correct and the composition disabled the defence.
That is a worse defect than the one I described, and a different kind: not an
absent feature but an absent line, invisible in every type signature involved.

## Fixed

| Finding | Status |
|---|---|
| SLIMRPC sends no `a2a-version` (§3 MUST) | Fixed — and `validate_version_metadata` is now public, so the server half is fixable from outside the workspace at all |
| SLIMRPC leaks transport keys into A2A headers | Fixed, case-insensitively, taking two keys from `slim_rpc` itself |
| `SlimName::parse` rejects the spec's own bare form | Fixed — bare, `slim://` and `slimrpc://` all resolve to one name |
| 379 published dependencies unaudited | Fixed — `cargo deny` and `cargo package` now run on the binding. **Advisories: clean** |
| No header-read timeout | Fixed — the `Timer` is installed and the timeout is configurable |
| No idle timeout | Fixed — enforced at the socket, counting traffic in both directions so SSE survives |
| Incremental persistence delivers no asymptotic win | Fixed — append is now O(1) in document size |
| Nothing has ever been soak-tested | Fixed — 562,690 requests, 17.0 bytes/request, p95 flat; nightly in CI |

## Where the tests were the problem, not the code

Four tests passed against a defect they existed to catch, and each was found by
injection rather than by review:

* The slowloris test passed with the `Timer` removed — because
  `with_idle_timeout(None)` was a `Duration::MAX` sentinel, `Instant::now() +
  Duration::MAX` panicked in the connection task, and the peer saw a reset in
  94 ms. The disabled path was broken *and* the test passed because of it.
* The same test asserted an outcome ("the read completed") where the property
  is a reason. A connection dropped by a panicking task completes the read too.
* The SSE test never read, so it never reached the deadline check, and passed
  with writes not counting as activity at all.
* Nothing asserted the artifact journal is ever emptied. `splice` skips
  positions the document already holds, so a `save` that failed to clear it
  returns correct tasks forever while the table grows without limit — invisible
  to any test that only checks what comes back.

## Still open

* ~~**Horizontal scaling.**~~ **Closed, and it was mostly not what I said it
  was.** Measured with two handlers over one PostgreSQL
  (`tests/multi_replica.rs`): task visibility crosses replicas, and so does
  subscription *termination* — the reattach hook polls the shared store, so
  §3.1.6 holds across replicas, which I had assumed rather than checked.
  Intermediate events do not cross, which is architectural and now pinned by a
  test. The one real defect was the rate limiter, and
  `RateLimitInterceptor::with_shared_counter` closes it. Two replicas under
  sustained load is now measured too — 64,642 request pairs, 0 failures, 0
  cross-replica misses, 19.2 bytes/pair, p95 flat at 1.04x. See Addendum 3 for
  the two findings that run turned up.
* **MSRV 1.93 against the official SDK's 1.85.** A decision, not a defect.
* **No CLI.**
* **No troubleshooting page**, and no chapter for multi-tenancy, observability
  or security.
* **No example uses SLIMRPC**, and `multi-lang-team` still has no Rust worker.
* **`send_message_inner` is 526 lines.**
* **`mutants.toml` is still at the wrong path.**

---

# Addendum 2: the horizontal-scaling gap

Called out in the first addendum as "the largest remaining gap". Measured, it
was four separate questions with four different answers, and only one of them
was a defect.

| Claim | Measured | |
|---|---|---|
| A task created on replica A is readable on B | **works** | the store holds it |
| `GetTask` / `ListTasks` span replicas | **works** | same |
| A subscription on B terminates when the task finishes on A | **works** | reattach hook polls the store; §3.1.6 holds across replicas |
| B sees A's intermediate events | **does not** | queues are per-process; architectural |
| The configured rate limit is the deployment's | **did not** | each replica admitted the full rate |

I had reported this as one gap. Three quarters of it already worked, and the
part about subscriptions being spec-compliant across replicas was something I
assumed rather than verified in either direction.

## The fix, and what it costs

`RateLimitCounter` is a one-method trait; `PostgresRateLimitCounter` implements
it with a single atomic `INSERT .. ON CONFLICT DO UPDATE .. RETURNING`. Two
limiters sharing one now admit 5 where they used to admit 10.

The cost is not small and is documented rather than buried:

| Counter | Per request |
|---|---:|
| In-process (default) | 0.2 µs |
| `PostgresRateLimitCounter` | 232 µs |
| The same on a durable pool | 598 µs |

A whole JSON-RPC request through this server measures ~195 µs on the same
machine, so a shared counter roughly doubles a request. The 598 → 232 µs comes
from running the counter's own pool with `synchronous_commit = off` — correct
for a counter describing a 60-second window, and deliberately *not* applied to a
pool passed in via `from_pool`, which is usually the task store's.

When the counter is unreachable the limiter counts locally, so the failure mode
is the per-process behaviour the deployment already had — not an outage, and not
an open door.

## A fifth test that passed while testing nothing

The fallback test built its "unreachable" counter by dropping the database
underneath it. The drop silently failed — Postgres needs `WITH (FORCE)` when
pools are open, which the existing store suite already knew — so the counter
kept working and the test passed having exercised no fallback at all. It also
leaked seven scratch databases.

That is the fifth test in this branch to pass against the defect it existed to
catch, and the third to do so because it asserted an outcome that was true for
the wrong reason. The fallback is now tested against a stub counter that always
fails, which is where interceptor behaviour belongs anyway.

## Correction: the "157 ignored tests"

The original review recorded 157 ignored tests and called them database-gated.
Run against live PostgreSQL 16.13, the real split is **24 genuinely runnable
ignored tests, all passing** — 23 Postgres plus the soak. The other ~134 are
`ignore`-marked documentation and book blocks: illustrative fragments that are
not tests at all. The repository tracks them in `.book-ignore-baseline`, and I
should have read that before characterising them.

---

# Addendum 3: two replicas under sustained load

The last unevidenced item. A 60-second run, 8 workers round-robining across two
replicas over one PostgreSQL, each request writing on one replica and reading
back from the other:

| | |
|---|---|
| Request pairs | 64,642 |
| Failures / cross-replica misses | 0 / 0 |
| Resident growth after warm-up | 19.2 bytes per pair |
| Rate-limit table, peak | 16 rows for 8 callers across ~12 windows |
| p95 latency, first quarter → last | 9,333 µs → 9,707 µs (1.04×) |

The shared counter's table staying bounded is the assertion only a long run can
make, and it has teeth: disabling the sweep takes it to 104 rows against a
ceiling of 32.

## ~~Finding~~ Fixed: `caller_identity` was never set by anything

The rate limiter documents `CallContext::caller_identity` as its first choice of
caller key, "set by authentication interceptors". Nothing set it:

* `ServerInterceptor::before` takes `&CallContext` — an interceptor
  *structurally could not* mutate one;
* no dispatcher constructed a context with an identity;
* `with_caller_identity` had no caller outside tests.

So every caller shared the `"anonymous"` bucket unless `trusted_proxy_hops` was
configured with an `x-forwarded-for` header present. **Per-caller rate limiting
was not per-caller**, and one noisy client spent everyone's budget. It went
unnoticed because every rate-limit test set the identity by hand on a
`CallContext` it had built itself.

The `JwtAuthInterceptor` had already run into this and left a note — it
computed a validated principal and threw it away, `map(|_principal| ())`, with
a comment saying the principal "is available should a future `CallContext` gain
a slot for it". That slot is the fix.

`CallContext::caller_identity` is now a `OnceLock<String>` with
`set_caller_identity(&self, ..)`, so the component that knows who the caller is
can say so through the shared reference it actually has. Write-once rather than
a mutex because identity is established once; two interceptors disagreeing is a
misconfiguration, and the first answer now sticks instead of the last one
silently winning.

Three interceptors supply one. `JwtAuthInterceptor` records the validated `sub`,
which closes its own comment. `ApiKeyAuthInterceptor` and
`BearerTokenAuthInterceptor` record a label when built with
`with_labelled_keys` / `with_labelled_tokens`.

The label is supplied rather than derived from the credential, deliberately: a
caller key reaches a shared rate-limit table, and can reach logs and metrics. A
bearer token belongs in none of those, so hashing it — the obvious shortcut —
would have put credential-derived material in all three.

One ordering requirement falls out, and it fails silently: the chain runs
interceptors in registration order over one context, so a limiter registered
before authentication reads an identity nothing has set yet and buckets every
caller together. Documented on both the module and the struct.

Verified under load: the two-replica soak now authenticates each of its eight
workers with its own labelled key rather than working around the gap with
`x-forwarded-for`, and the counter table holds eight keys across 63,339 request
pairs.

## Finding: the `tasks` table grows without bound

65,399 rows in one minute, and nothing removes them. A2A has no `DeleteTask`
and `PostgresTaskStore` has no capacity eviction — both correct decisions — so
a deployment needs a retention policy it has to supply itself. Nothing in the
repository said so; the horizontal-scaling chapter now does.

## Correction: what CI actually caught that I did not

Nine CI jobs went red on this branch from four causes, and the instructive one
is that **I had been verifying with `--all-features` throughout**. An import
used only inside a `#[cfg(feature = "postgres")]` module is an unused import
without that feature, and `-D warnings` makes it an error — invisible to every
local check I had been running. Both feature sets are now in my loop.

The other three: three rustdoc links to items that were `pub` but never
re-exported (the doc error was the symptom of a real omission); an auto-trait
regression only `cargo semver-checks` could see, where `Arc<dyn RateLimitCounter>`
cost `RateLimitInterceptor` its `UnwindSafe` — now asserted, with a
compile-time guard so the next one is a build error rather than a CI surprise;
and benchmark prose that disagreed with its own tables by 0.1 µs, which
predates this branch.

One thing left for a maintainer: the semver job's comment says twice that it
"reports rather than hard-fails until the first 1.0", but it carries no
`continue-on-error`, so it blocks. I fixed the regression rather than relaxing
the gate.

---

# Addendum 4: a flake I wrote, and a CI setting that hid where it was

## The failure

`Test (1.93, macos-latest)` went red on
`serve::graceful::idle::tests::a_slow_but_progressing_peer_is_not_disconnected`:

```
byte 2 should arrive, got: connection idle for more than 75s; closing to
release the task and descriptor
```

The peer had written that byte 15 virtual seconds inside the window, and the
same test had passed on Linux and Windows, on both toolchains, on every run
before it — including 300 consecutive local runs afterwards. It is my own test,
added with the connection timeouts in `b27d400`, so this is a flake the branch
introduced and got away with once.

## Why paused time and a real socket do not mix

The module claimed `start_paused` made these tests immune to a loaded runner.
That is true of the *timers* and false of the *transport*, and the difference
is in tokio's own auto-advance (`runtime/time/mod.rs`, `park_thread_timeout`):

```rust
self.park.park_timeout(rt_handle, Duration::from_secs(0));
if !handle.did_wake() {
    clock.advance(duration);
}
```

The runtime polls the I/O driver with a zero timeout and then jumps the clock to
the next timer **regardless of what that poll found** — `did_wake` belongs to
the *time* handle, so dispatching a socket readiness event does not stop the
advance. A peer's `write` reaching the kernel and the reader learning the socket
is readable are two different moments, and the virtual clock is free to cross
the idle deadline in between. When it does, the reader polls the socket
(Pending, because readiness has not arrived), polls the deadline (expired), and
reports the connection idle. On epoll the event was always there in time; on
kqueue, once, it was not.

The fix is `tokio::io::duplex`, which removes the gap rather than narrowing it:
a write wakes the reader's waker from inside `poll_write`, so the reading task
is on the run queue before the scheduler can consider parking. Five of the six
tests moved; the sixth asks whether `is_write_vectored` is delegated, which only
a real socket can answer, and it does not pause time.

Widening the window instead — a 300-second idle allowance against 60-second
writes — would have made the race rarer and left it in place. A test whose
failure mode is indistinguishable from the defect it exists to catch is worse
than no test, because the first response to it is to doubt the code.

## A sixth test that was the problem, not the code

`end_of_stream_reads_as_eof_not_activity` asserted only the EOF half of its own
name. Making EOF re-arm the timer — the exact defect the guard in `poll_read`
exists to prevent — changed nothing that test could see, because at this layer a
re-armed timer has no observable effect on a read that already returned zero. It
now reads the deadline directly, before and after, and the injection that
proves it is in the set below.

All four gates are proven able to fail:

| Injected defect | Caught by |
|---|---|
| Reads do not re-arm the timer | `a_slow_but_progressing_peer_is_not_disconnected` |
| Writes do not re-arm the timer | `a_write_only_stream_stays_alive` |
| The deadline is never consulted | `a_silent_peer_is_disconnected`, `activity_does_not_buy_permanent_immunity` |
| EOF counts as activity | `end_of_stream_reads_as_eof_not_activity` |

## The CI setting that sent me to the wrong file

The run reported six failing `Test` jobs. Five were `cancelled`, not failed:
the matrix had `fail-fast` at its default of `true`, so one macOS failure
cancelled the other five mid-step. I opened one of the cancelled logs first,
found four of five tests in `hostile_server_tests` reporting `ok` and the fifth
never finishing, and spent the first part of the investigation on a hang that
never happened — the job had simply been killed while that binary was running.

`fail-fast: false` now applies to both the `Test` and `Clippy` matrices, which
is what `fuzz`, `mutants` and `tck` already do. A red matrix should say which
cells are red. The runner-minutes it saves are not worth a log that reads like
a different bug.

## RUSTSEC-2026-0258, and why the advisory's own fix broke a different gate

The same run went red on both cargo-deny jobs — the workspace's and the
binding's — from an advisory published between it and the run before it:

> **RUSTSEC-2026-0258** — h2 accepts and queues empty DATA frames without
> limit. If streams are not actively drained this is unbounded memory growth,
> or a panic if the length overflows. Patched in 0.4.16.

h2 sits under hyper on this SDK's HTTP/2 request path, so it is a real finding
rather than a transitive curiosity, and the fix is a lockfile bump: the
workspace was on 0.4.13, the binding on 0.4.15.

The advisory's suggested remediation — `cargo update -p h2` — does more than it
says. On cargo 1.94.1 it also **downgraded `windows-sys` for seven unrelated
crates**, from 0.61.2 to 0.52.0, 0.60.2 and 0.48.0, which split
`windows_i686_gnullvm` across two versions and failed the `bans` check that had
just passed. `--precise 0.4.16` made no difference: the collateral change is a
re-resolution artifact, not something the version selector was choosing. So the
advertised fix for a security advisory turns a green supply-chain gate red, on
a line nobody would think to read in a lockfile diff.

Both lockfiles were therefore edited surgically — h2's version and checksum,
four lines in total, with h2 0.4.16's dependency list confirmed identical to
0.4.13's first. `cargo metadata --locked` accepts both unchanged, which is the
check that would have caught a hand-edit that did not resolve.

Worth noting what found this: the binding's own `cargo deny`, added earlier in
this branch, on the tree that had been shipping 379 unaudited transitive
dependencies. It went from being a gate with nothing to say to catching a live
advisory within a day.

## The semver gate said it was advisory. It has been blocking since 0.7.0

The `cargo-semver-checks` job carried the claim twice — once in its header
("this reports rather than hard-fails until the first 1.0"), once inline beside
`feature-group`, a key that selects features and has nothing to do with
blocking. Neither was implemented. There is no `continue-on-error` in the job,
so every break it found has been a hard failure for as long as the comment has
denied it.

The fix is the comment, not the behaviour, because the premise does not survive
being run. The stated reason for wanting it soft — "on a 0.x baseline a MINOR
bump is allowed to break" — describes a rule cargo-semver-checks already
applies. Measured with 0.50.0 against `a2a-protocol-types`, renaming
`utc_now_iso8601` as the injected break:

| state | exit | verdict |
|---|---|---|
| version unchanged, no break | 0 | no semver update required |
| version unchanged, break present | 100 | semver requires new major version |
| 0.9.0 → 0.10.0, same break | 0 | "(major change)", 254 checks skipped |

A deliberate 0.x break passes the moment the minor component is bumped. What
`continue-on-error` would have bought is already there; adding it would only
buy the rest — a gate that cannot fail. What the job can still fail on is a
break the version does not cover, which is the case worth blocking and the
likely one here: `crates/*/Cargo.toml` reads 0.9.0 and crates.io has 0.9.0, so
between releases the baseline is the version being developed against.

(`--release-type minor` does *not* reproduce the third row — it reports
"requires new major version" for a 0.9.0 crate. Only a real 0.9.0 → 0.10.0 bump
is read as a major change. The override is not a model of the release.)

Worth knowing rather than assuming: `feature-group: default-features` checks
the default surface only, so a break behind `signing`, `grpc`, `postgres` or any
other optional feature is outside what this gate can see. Left as-is and
written down.

### What let the claim stand: a coverage list that overstated itself

`semver` was listed in `GATE_JOBS` in both `scripts/preflight.sh` and
`scripts/prove_gates_fail.sh` — the set whose comment promises preflight runs
them, and which prove_gates_fail exists to prove can fail. It contributed
nothing to either. Both scripts extract gates from `run:` steps, and `semver`'s
whole body is a marketplace action; so is `deny`'s. Two of the ten listed jobs
yielded zero gates and the listing said so nowhere. `prove_gates_fail.sh --list`
prints 45 gates; none of them is semver or cargo-deny.

`require_known_jobs` already guarded one direction of this — every ci.yml job
must be classified as gate or exemption, and it fails loudly otherwise. The
missing half was whether the classification is *true*. `require_nonempty_gate_jobs`
now asserts that every job in `GATE_JOBS` yields at least one gate; `deny`,
`semver` and `slimrpc-binding` move to the exemption list with their reasons.
Proven able to fail by putting `deny` back and watching it refuse to run.

And `--list` printed its own job set from a hardcoded string reading
"fmt, clippy, test, doc", which had not mentioned `test-postgres`, `package`,
`dogfood` or `example-surface` since they were added — the header under-reported
the script's coverage by half, in the output a reader consults to learn what it
covers. It is derived from `GATE_JOBS` now.

### One of these was mine

`require_known_jobs` was not merely guarding: it had been *firing*, and had
made `preflight.sh` exit 2 without running a single gate since `71592b6` —
my own commit, which added the `slimrpc-binding` job to ci.yml and told
neither script about it. The guard worked exactly as designed and nothing ran
it for six commits. A guard nobody runs is a comment.

`slimrpc-binding` is exempted rather than integrated, with the reason recorded:
every one of its `run:` steps is reached by `working-directory:`, which neither
parser models, so listing it as a gate would run `cargo fmt --check`, `cargo
clippy` and `cargo test` from the repo root — checking the workspace a second
time, the binding not at all, and reporting the result as the binding's
coverage. Teaching the parser `working-directory`, plus injections in
prove_gates_fail.sh that reach an out-of-workspace tree, is the real fix and is
not done here. Until then CI is the only thing checking that crate.

## The SLIMRPC binding now has local gates, and they are proven able to fail

The exemption written one commit earlier is gone. Both `scripts/preflight.sh`
and `scripts/prove_gates_fail.sh` now model `working-directory:`, which is the
only reason the binding was unreachable: it is a separate cargo project with its
own lockfile, and every one of its `run:` steps is reached that way.

The parser has to *hold* the directory rather than read it in place —
`working-directory` precedes `run:` inside a step, and the flush the run rule
performs would clear it. In `prove_gates_fail.sh` the resulting `cd` prefix joins
the env field rather than the command, so `injection_for` still matches a bare
command. preflight already ran each gate as `(cd "$REPO_ROOT" && eval "$cmd")`,
a subshell, so the `cd` cannot leak into the next gate.

Both go from 45 gates to 50. Measured, with
each gate passing its baseline first — the script scores nothing PROVEN without
a green baseline, which is the check that caught a permanently-red `doc` gate
being recorded as proven in August:

| gate | injected defect | verdict |
|---|---|---|
| `cargo fmt --check` | misformatted fn | PROVEN, exit 1 |
| `cargo clippy --all-targets -- -D warnings` | `deref_addrof` | PROVEN, exit 101 |
| `cargo build --bin slim-node` | unresolved type | PROVEN, exit 101 |
| `cargo package --no-verify --allow-dirty` | dangling readme | PROVEN, exit 101 |
| `cargo test -- --test-threads=1` | panicking test | PROVEN, exit 101 |

`5 proven, 0 unproven`, tree clean afterwards.

Two of the five needed injection kinds that did not exist. `cargo build` denies
nothing, so the lint probe every clippy gate uses would compile and the gate
would go green — it takes a real compile error, and one in the binary target
rather than the library, because `--bin slim-node` is what the step names. And
the binding packages with `--allow-dirty`, so unlike the workspace `package`
gate it needs no clone; its manifest declares no `readme`, so one is inserted
rather than repointed, the insert guarded against the TOML duplicate-key
mistake that the workspace arm already documents.

`injection_for` routes on the working directory before the command text. The
binding's clippy gate is `cargo clippy --all-targets -- -D warnings` and the
workspace one is that plus `--workspace` — close enough that matching on text
would eventually hand a `bindings/` gate an injection written for `crates/`,
and it would report UNPROVEN while being perfectly able to fail. A false
negative in the tool whose whole job is catching false negatives.

Still skipped, and now visible rather than assumed: the two `run: |` blocks
(SPIRE install, the SPIFFE suite). The parser reads single-line `run:` only, and
preflight's block-scalar note names `slimrpc-binding` alongside `test-postgres`
so a reader is told which jobs it is not replicating.

One thing this run demonstrated by accident. A commit landed while the prover
was mid-injection, and `git status` at that moment read
`M bindings/a2a-protocol-slimrpc/src/lib.rs` — the live clippy probe. Staging by
explicit path kept it out; `git add -A` would have committed it, which is
exactly how probe code reached three commits the first time this script ran.
The warning it prints on startup is not decoration.

## Multi-line `run:` blocks, and one parser instead of two

The last thing neither local harness could see. `gates_for_jobs` read
single-line `run:` only, so ci.yml's three block scalars were skipped in both:
the multi-replica postgres suite, the SPIRE install, and the SPIFFE suite.
preflight printed a note saying so, which is better than silence and still not
coverage.

Block bodies are now emitted as one `bash -e -c $'...'` command — one physical
line because every consumer is line-oriented, ANSI-C quoted rather than joined
on `;` because two of the three are a single logical command split by a
backslash continuation, where `;` would make two broken commands out of one
working one.

`bash -e`, deliberately not `-eo pipefail`. That is the shell GitHub gives a
`run:` step on Linux, and the gap between the two is how the official-TCK gate
came to print REGRESSION and exit 0. All three semantics were checked before
being relied on: continuation joins, `-e` aborts, `false | tee` still exits 0.
A harness that runs the block under a stricter shell than CI is not reproducing
the gate, it is inventing a different one.

Not every step is a gate. `Install SPIRE` downloads a tarball and hands the
path to the *next* step through `$GITHUB_ENV`, inter-step state only GitHub
implements; standalone it has nothing to assert and, under its own `set -u`,
nothing to append to. `SKIP_STEPS` exempts it by name with the reason beside
it, and the note now prints what is exempted rather than a blanket disclaimer.

Both new gates were proven against real prerequisites rather than accepted as
PRE-BROKEN — PostgreSQL 16.13 on the URL the job uses, and SPIRE 1.11.2
installed by running the exempted step's own script:

| gate | injected defect | verdict |
|---|---|---|
| `--test multi_replica -- --ignored` | ignored test that panics | PROVEN, exit 101 |
| `--test spiffe{,_federation,_rotation}` | ignored test that panics | PROVEN, exit 101 |

`2 proven, 0 unproven`. Both injections had to be `#[ignore]`d tests in the
files those gates name, because that is how the gates select: the
always-compiled probe the other test gates use would be filtered out before
running, and the gate would pass with the defect in the tree.

### Three bugs in the above, all mine, all found by testing rather than reading

**The quoting.** gsub applies its own backslash rules on top of the string
literal, and the two layers together turned one backslash into six. Rebuilt a
character at a time.

**The guard that checked nothing.** `require_known_skips` iterated
`printf '%s' "$SKIP_STEPS" | tr '|' '\n'` with `while read`. No trailing
newline means `read` returns non-zero on the final line, so the body never ran
for the last alternative — and with a single exemption, that is every
alternative. The guard executed and verified nothing. Found by handing it a
deliberately stale pattern and watching it pass.

**Folded scalars.** The rule matched `run: [|>]` and treated both alike. YAML
folds `>` by joining lines with spaces, so `run: >` / `echo one` / `two` means
`echo one two`; the parser would have emitted two commands, the second of which
does not exist. Confirmed against PyYAML rather than assumed. Folding is
implemented now — see below; the first fix for this was a refusal, which was
the wrong shape of answer.

### One parser, not two

The 500-line ratchet caught `preflight.sh` at 586 lines and was right to. Its
message offers splitting or a recorded exemption, and splitting was the honest
option, because the thing to split out was a duplicate: `gates_for_jobs` existed
in both scripts, byte-identical apart from one emit line, kept in step by a
comment reading "same job set and parser as scripts/preflight.sh, deliberately".
Four changes in one day had to be made twice. Nothing would have failed if one
copy had been missed — the two scripts would simply have disagreed about what
CI runs, silently, which is the class of defect both exist to catch.

`scripts/lib/ci_gates.sh` now holds the parser, the job and step lists, and the
guards. preflight.sh drops 586 to 385 lines, prove_gates_fail.sh 1197 to 998.

**Correction.** Earlier entries in this document said preflight covered 48 then
53 gates. Both were three too high: the count came from `grep -c '^  '` over
`--list`, which also matches the three tier-summary lines it prints at the end.
The true figures are 45, then 50, then 52, and both tools cover the same 52 —
they share one GATE_JOBS now, so they cannot differ. preflight has been
printing the right number in its own `--full` line the whole time.


## Folding, implemented rather than refused

The first response to the folded-scalar bug was to make `gates_for_jobs` refuse
`run: >` and say so. That is defensible — a gate running the wrong command is
worse than a missing one — but it is still a hole with a sign on it, and the
sign said "someone else decide". So `>` is now folded properly:

* a break between two ordinary lines folds to a single space;
* empty lines are not folded away — k empty lines between content become k
  newlines, and the fold-space is dropped;
* a **more-indented** line is kept literally, and the breaks on either side of
  it stay newlines;
* an explicit indentation indicator (`>2`, `|2`) sets the block indent instead
  of inferring it from the first non-empty line, which is the case it exists
  for.

Chomping (`-`, `+`) is parsed and deliberately not acted on: it changes only
how many line breaks end the scalar, and a trailing newline cannot change what
a shell command does. Said out loud here rather than left as an accident.

The more-indented rule cost a second bug. Getting it right for a bare indented
continuation is easy; what is not obvious is that a blank line *before* one
contributes its newline **and** the unfoldable break survives — one blank line
before an indented continuation is two newlines, not one. That was wrong in the
first implementation and neither reading the spec nor reading the code found
it.

What found it was `scripts/check_block_scalars.py`, which is now a CI gate: 22
block-scalar shapes, each parsed by the awk and by PyYAML, compared. It is a
differential test rather than a table of expected strings on purpose — the
expectations come from an implementation that is already right, so the test
cannot inherit the author's misreading, which is exactly how the bug survived
being written and reviewed.

`bash` decodes the `$'...'` quoting in that check rather than the checker
re-implementing it, so the emitter is tested against the shell that will
actually run it and not against a second copy of its own logic.

Proven able to fail, by widening the fold separator to two spaces in
`ci_gates.sh` and watching every folded case go red: `1 proven, 0 unproven`.
The injection breaks folding rather than the checker, because the gate exists
to notice a parser regression.

## The tasks table: settled

I called this an operator decision four times and left it. That was half right
— the *interval* is the operator's, and it has legal inputs as often as
engineering ones — but "we have no mechanism and no stated default" is not a
policy, and calling it one was a way of not doing the work.

The finding, once I looked properly, was not the one I had been describing.
The SDK does have task retention: `TaskStoreConfig` defaults to a one-hour TTL
and a 10,000-task cap, and `InMemoryTaskStore` honours both. What it does not
have is any connection between that config and the persistent stores —
`SqliteTaskStore::new` and `PostgresTaskStore::new` take a URL and nothing
else, and never read it. So the default in-process deployment forgets a task an
hour after it finishes, the durable deployment keeps it forever, and neither
behaviour was written down anywhere an operator would look. **The divergence
was the defect**, not the growth.

### The decision

**A persistent store deletes nothing unless asked. `purge_expired` is how you
ask.**

Forgetting is right for a cache and wrong for a database. A library that
quietly deleted rows from an operator's PostgreSQL would be a far worse
surprise than one that grows. So the default stands, is now stated in the
module docs, in the deployment chapter and in the type itself, and the
mechanism exists for any other answer.

Not wired to a timer inside the store, deliberately: a sweep that fires on its
own fires during your traffic peak, and the store does not know when that is.

### How fast it actually grows

The page said "grows without bound" and left the reader to guess whether that
meant next week or next decade. Measured, for a completed task with a
two-message history and no artifacts, including the indexes the store creates:

| store | bytes/row | 1M tasks |
|---|---|---|
| PostgreSQL 16.13 | 826 | 0.77 GiB |
| `SQLite` | 781 | 0.73 GiB |

Under a gigabyte per million completed tasks. The answer to "is this urgent" is
volume, not time — and now it is a number rather than an adjective.

### What the implementation had to get right

*Only terminal tasks.* `Completed`, `Failed`, `Canceled`, `Rejected`. A task in
`InputRequired` is a workflow waiting on a human, not a leak, and is never
eligible at any age. The in-memory store *does* evict non-terminal tasks under
capacity pressure, because RAM is a hard bound; disk is not.

*`rowid`, not `id`.* `tenant_tasks` is keyed on `(tenant_id, id)`, so the same
task id exists under every tenant. `DELETE ... WHERE id IN (...)` reads
correctly and would have taken one tenant's expired task **and every other
tenant's task of the same name** — silent cross-tenant data loss, triggered
only once two tenants happened to pick the same id. There is a test for exactly
that, on both backends.

*The batch is chosen inside the `DELETE`.* A select-then-bind-back-the-ids
design would hit SQLite's variable limit (999 before 3.32) at precisely the
batch sizes someone would reach for on a first sweep through years of backlog.
A `rowid`/`ctid` subquery keeps the bound parameters at six however large the
batch.

*The clock is the database's.* `strftime('now', ?)` and `now() - $2::interval`,
not a cutoff formatted by the caller: a host running fast would otherwise
delete work younger than the policy allows.

*Journal orphans.* `task_artifact_appends` has `ON DELETE CASCADE`, and
`journal.rs` already records that `from_pool` cannot assume a caller's pool has
`foreign_keys=ON`. So the sweep clears orphans explicitly. Both branches are
tested — with the pragma on, the cascade takes them and the sweep reports
**zero**, which is the healthy reading; with it off, the sweep reclaims them
and says so.

### Three things the work surfaced that I had not expected

**`TaskState` is `#[non_exhaustive]`**, so no downstream crate can match it
exhaustively — which meant my first version of the terminal-state list was a
hand-written array that would keep compiling, and keep looking right, after a
new terminal state was added that it did not mention. Fixed by adding
`TaskState::ALL` to the types crate with a guard that lives where
exhaustiveness is enforceable: an exhaustive `match` assigning each variant an
index, and an array sized by `ALL` that panics if a variant has an index but no
entry. Adding a variant now fails to compile, and adding it to the match
without adding it to `ALL` fails the test.

**`journal_append` never touches the `tasks` row.** Not `state`, not
`updated_at`. That is correct for the delta optimisation — the document is
reconstructed by splicing — and it means a terminal state reached *via* an
append would leave a stale `state` column that both `list(state=…)` and this
sweep would read. It cannot happen on the real path, because the status
transition that makes a task terminal is a full `save`. Worth knowing rather
than discovering.

**The book gate caught an API mistake, not a prose one.** `purge_expired`
first returned `Result<_, sqlx::Error>`, so the doctest for the deployment page
failed to compile for want of `sqlx` — which is the point: every other store
method returns `A2aResult`, and mine was leaking the driver's error type into
callers' signatures. The gate that compiles the book found an API design
problem that no test of mine was looking for.

Verified: 12 `SQLite` retention tests, 3 against a live PostgreSQL 16.13, the
full 26-test ignored postgres suite and the 6-test multi-replica suite still
green, `cargo test --workspace --all-features` 2,906 passed, default 2,898,
clippy clean on both feature sets, and the book's own doctest for the new API
compiles.

---

# Addendum 5: three failures of one kind, and a gate that does not run on push

The retention work landed in three commits, and two of them exist only because
the first was wrong. All three failures were the same mistake wearing different
clothes, and the mistake was in how I verified, not in what I wrote.

## The class: I checked two points on an eleven-point matrix

`RUSTFLAGS: -D warnings` is set once, at the top of `ci.yml`, and applies to
every job. Fifteen jobs went red on the first push because
`terminal_state_labels` and `effective_batch_size` are crate-internal with only
feature-gated callers: with no backend enabled they are dead code, and dead code
is an error under that flag. The part I had not internalised is that the flag
applies to `a2a-protocol-server` when it is built *as a dependency* — which is
why the failures included `cargo clippy -p a2a-protocol-client` and the SLIMRPC
binding, neither of which mentions retention.

The second failure was the same shape one layer up: the module doc example named
`SqliteTaskStore`, and this crate has no default features, so the doctest failed
to compile in every configuration that does not ask for a backend. Both
configurations I had tried by hand happened to enable one.

`scripts/preflight.sh --full` found that second one before CI reported it. I had
built most of that script earlier in this branch specifically to run CI's
commands with CI's environment, and then did not run it before pushing. It works;
I did not use it.

## What `cargo package` actually resolves, and why it matters here

The third failure is the one worth keeping, because nothing about it is
guessable from the source.

`cargo package` strips path dependencies when it builds the tarball, then
verifies that tarball against the **registry**. The verification log says so if
you read the line rather than skim it:

```
Verifying a2a-protocol-server v0.9.0
   Compiling a2a-protocol-types v0.9.0                     <- no path: crates.io
   Compiling a2a-protocol-server v0.9.0 (/…/target/package/…)
```

So the packaged server compiles against **published** `a2a-protocol-types`
0.9.0, which does not contain the `TaskState::ALL` this branch adds. The general
rule: *a new cross-crate API cannot be consumed by library code in the same
release cycle that introduces it.* `--workspace` packaging does not rescue this
when the sibling version is already published — cargo takes the registry copy.
The gate would have stayed red on every PR until a version bump.

The fix keeps the guard and drops the dependency: `cargo package` builds the
library and not the tests, so a `#[cfg(test)]` reference to `TaskState::ALL`
compiles against the workspace copy, costs nothing at packaging time, and still
fails the moment `TERMINAL_STATES` stops agreeing with `is_terminal()`.

### Correction to Addendum 4

Addendum 4 says the hand-written terminal-state list was "fixed by adding
`TaskState::ALL` to the types crate". That describes the guard inside the types
crate, which still stands. It also implies the *server* derives its list from
`ALL`, and that is no longer true: the server keeps a local `TERMINAL_STATES`
and the agreement is asserted in a test, for the packaging reason above.

## A gate that does not run on push

Twenty-two CI jobs were green on the final push, and the branch would still have
failed on opening a pull request. `dco.yml` triggers on `pull_request` only, so
nothing about a green push tells you anything about it.

It checks two things, and this branch failed both on 13 of its 28 commits: the
author must not be a tool identity (`noreply@anthropic.com` is named explicitly
in the workflow), and there must be a `Signed-off-by` trailer matching the
author. The session's git identity had drifted partway through the branch —
commits up to `fix(server): make per-caller rate limiting actually per-caller`
are authored correctly and signed off, everything after is not. PROVENANCE.md
§3.2 documents the convention and the exact `git config` to set; the workflow
prints the exact rebase to repair it.

Repaired by re-authoring the affected commits and normalising the assistant
trailer to the `Co-Authored-By: Claude <noreply@anthropic.com>` form §3.2
documents — which is also main's dominant form, 47 commits to 7. The tree is
byte-identical before and after; only authorship and trailers changed.

**For anyone picking this up: a green push is not a green PR.** `dco.yml` and
the semver gate behave differently on the two events. Check the workflow
triggers before believing a push.

## Where to pick up

*Superseded by Addendum 6's "Where to pick up", which folds these four in. They
are all still open.*

Open, in rough order of how much they would repay:

* **`TaskState::ALL` cannot be used by server library code until the types crate
  is published with it.** After the next release, the `#[cfg(test)]` indirection
  in `store/retention/mod.rs` can collapse into a direct derivation. Nothing
  breaks if it never does.
* **Retention ships no scheduler, by design.** If users ask for one, it belongs
  behind a feature flag, not inside the store.
* **`.cargo/mutants.toml`.** The root `mutants.toml` is never read — cargo-mutants
  looks in `.cargo/`. CONTRIBUTING.md §370 documents this. Moving it changes what
  the mutation gate examines, so it needs a measured run, not a file move.
* **MSRV 1.93 against the official SDK's 1.85.** A decision, not a defect.
* **No CLI. No troubleshooting page**, and no chapter for multi-tenancy,
  observability or security.
* **No example uses SLIMRPC**, and `multi-lang-team` still has no Rust worker.

---

# Addendum 6: eleven questions, answered against measurements

**Date:** 2026-08-19. **Base:** `main` @ `4ddb7ab`. **Hardware for every timing
below:** 4 vCPU, 15 GiB RAM, `rustc 1.94.1`, Linux 6.18.5.

This is an addendum rather than a successor document, deliberately. Four of the
answers below are verdicts on §2.1–§2.5 and §3 of this same document, and a
correction that lives in a different file is a correction most readers will
never meet. The convention this document already uses — record the correction
beside the claim — is the reason its three previous corrections were worth
anything.

## How to read this

Every claim carries a label:

* **MEASURED** — I ran the command shown and this is its output.
* **OBSERVED** — I read it in the code, config or API response shown.
* **INFERRED** — reasoning from the two above, and marked as such.
* **UNVERIFIED** — carried forward from an earlier session or an external
  source, and *not* re-checked. §"What I did not verify" lists all of these in
  one place so the next session inherits known-unknowns rather than assumptions.

Where a question invites an opinion I give one recommendation and its cost, not
a menu.

---

## What I re-verified from the baseline, and where it was wrong

MEASURED, all of it. The commands are inline.

| Claim carried in | Verdict | Measurement |
|---|---|---|
| 2,906 tests at `--all-features` | **holds** | `cargo test --workspace --all-features` → `passed=2906 failed=0 ignored=168` |
| 4 crates at 0.9.0; SLIMRPC binding never published | **holds** | `curl crates.io/api/v1/crates/a2a-protocol-slimrpc` → `crate ... does not exist` |
| MSRV 1.93 | **holds** | `grep rust-version Cargo.toml` → `rust-version = "1.93"` |
| Examples, by source LOC | **holds** | `find examples/<n> -name '*.rs' \| xargs cat \| wc -l`; agent-team 8,911 … hello-agent 142 |
| Book ≈69,788 words | **holds** (69,787) | `cat book/src/**/*.md \| wc -w` |
| Book "~38 chapters" | **wrong, understated** | `book/src/SUMMARY.md` lists **45** chapters plus an unnumbered introduction; `find book/src -name '*.md' \| wc -l` → 51 |
| "Zero chapters match troubleshoot / multi-tenan / observab / security / monitor" | **holds for chapter titles; misleading as stated** | none of the five appears in `SUMMARY.md` (`grep -ic` → 0 for each), but the terms appear in 19, 7, 16 and 7 book files respectively. There is no *chapter* on these subjects; it is not true that the book is silent on them. |
| SLIMRPC: 11 methods everywhere | **holds** | `Method::ALL` has 11 entries; `grep 'rpc ' proto/a2a_v1/a2a.proto` → 11 |
| 13 workflows | **holds** | `ls .github/workflows/*.yml \| wc -l` → 13 |
| Downloads: types 27,045 / server 1,071 / client 1,016 / sdk 810 | **holds to the day** | crates.io API, 2026-08-19 |

Two corrections worth stating plainly, because both were in the flattering
direction: the book is **larger** than recorded (45 chapters, not ~38), and the
"zero chapters" line reads as "no coverage" when what is true is "no dedicated
chapter". I would rather the record said the second thing.

---

## The download figures, which are not an adoption signal

The brief asked me not to inflate this and not to dismiss it. Here is what the
numbers support.

**MEASURED** — `curl -sS https://crates.io/api/v1/crates/a2a-protocol-types/reverse_dependencies?per_page=50`:

```
meta: {'total': 4}
   a2a-protocol-types ^0.5    <- adk-server 2.0.0
   a2a-protocol-types ^0.9.0  <- a2a-protocol-server 0.9.0
   a2a-protocol-types ^0.9.0  <- a2a-protocol-client 0.9.0
   a2a-protocol-types ^0.9.0  <- a2a-protocol-sdk 0.9.0
```

Server: 2 reverse deps, both this workspace's own crates. Client: 1, this
workspace's. SDK: **0**.

So across four published crates there is **exactly one external dependent in the
entire registry**: `adk-server` (`zavora-ai/adk-rust`), and it pins
`a2a-protocol-types ^0.5` — four minor versions behind.

**MEASURED** — `curl https://index.crates.io/ad/k-/adk-server`, version 2.0.0:

```
features:  {"a2a-interceptors": [], "agent-registry": [], "default": []}
features2: {"a2a-v1": ["dep:a2a-protocol-types"], ...}
dep: a2a-protocol-types ^0.5 optional=True
```

The dependency is **optional and not in `default`**. Reaching it requires
`features = ["a2a-v1"]`.

**MEASURED** — the per-version download curve, `curl .../a2a-protocol-types/downloads`
(the endpoint returns a trailing ~89-day window, here 2026-05-22 → 2026-08-18):

| version | published | lifetime | in the 89-day window |
|---|---|---:|---:|
| 0.5.0 | 2026-04-02 | 22,513 | **22,009** |
| 0.7.0 | 2026-07-24 | 3,293 | 3,296 |
| 0.6.0 | 2026-06-10 | 838 | 838 |
| 0.8.0 | 2026-08-14 | 67 | 67 |
| 0.9.0 | 2026-08-17 | 35 | 35 |

A version published on 2 April that takes 97.8% of its lifetime downloads in the
89 days *after* 22 May does not have an organic adoption curve. Over the same
window `a2a-protocol-server` took 766 downloads across all versions — 2 to 30 a
day. Types is running at ~250/day. If those types downloads were users of this
project's SDK the ratio would be near 1:1, because the umbrella crate pulls all
four.

**INFERRED**, and I think unavoidably: the 27,045 figure on `a2a-protocol-types`
is dominated by traffic that is not adoption of this project — one downstream
crate's CI and registry mirrors, concentrated on one old version behind a
non-default feature flag.

**What the numbers do support:** 0.9.0 was fetched 35 times (types), 27 (server),
27 (client), 16 (sdk) in its first two days. `a2a-protocol-server` has 1,071
lifetime downloads. Those are the honest adoption figures and they are small.

**What they do not support:** any claim built on "27,000 downloads". The README
does not make one, and it should not start.

For context, **MEASURED** via `curl 'crates.io/api/v1/crates?q=a2a&per_page=40'`:
526 crates match "a2a"; `a2a-rs-core` has 15,275 lifetime downloads and `a2a-rs`
10,111. This is a crowded space, and this SDK is not the most-downloaded thing
in it.

---

## Q1 — What are we missing?

Against the A2A v1.0 specification, the official Rust SDK (`a2a-rs`), and the
official Python, JavaScript, Go and Java SDKs — the last four are not a paper
comparison here: `itk/agents/{python,js,go,java}-sdk` contain echo agents built
on each, and the in-repo TCK drives all of them (OBSERVED,
`itk/agents/js-sdk/package.json` pins `@a2a-js/sdk ^1.0.0`,
`itk/agents/go-sdk/go.mod` pins `a2a-go/v2 v2.3.1`,
`itk/agents/java-sdk/pom.xml` pins `a2a.sdk.version 1.0.0.CR1`).

Sorted into the three categories the brief asked for.

### Absent

**1. Server-side agent-card signing, and client-side verification.** MEASURED:

```
$ grep -rn 'feature = "signing"' crates/a2a-protocol-server/src/   # → nothing
$ grep -rn 'feature = "signing"' crates/a2a-protocol-client/src/   # → nothing
$ grep -rn 'verify_agent_card' --include='*.rs' . | grep -v types/  # → nothing
```

`a2a-protocol-types` has `sign_agent_card` and `verify_agent_card`, both well
tested (`crates/a2a-protocol-types/tests/signing_tests.rs`). Neither the server
nor the client contains a single line of `signing`-gated code. The server never
signs the card it serves; `resolve_agent_card` returns whatever it fetched
without looking at `signatures`.

Both crates nonetheless ship a `signing` feature, and the client's read
**"Enable agent card signing verification"** — which claimed an integration
that does not exist. Fixed in this branch: all three descriptions now say what
they actually do. The integration itself is backlog item **B4**.

Not verifying by default is *correct* — verification needs a trust anchor a
client cannot invent. The defect was the description, not the default.

**2. SSE-level stream resumption.** MEASURED: `build_sse_message_frame`
(`streaming/sse.rs:70`) emits `event: message\ndata: …\n\n` and no `id:`;
`grep -ri 'last-event-id' crates/ bindings/` returns nothing.

MEASURED against the spec (fetched 2026-08-19, <https://a2a-protocol.org/latest/specification/>):
A2A v1.0 says **nothing** about SSE `id:`, `Last-Event-ID`, or resumption. Its
answer to reconnection is `SubscribeToTask`, which this SDK implements, tests
and drives through the TCK. So this is a **deliberate scope boundary and not a
spec gap** — but nothing in the repository says so, which is why it reads as a
gap.

**3. A CLI.** Unchanged from §4 of this document. Evaluating this SDK still
means writing Rust and compiling it.

### Present but unreachable, or present and inert

**4. `SseFrame::id` and `SseFrame::retry`.** MEASURED: the parser populates both
(`sse_parser/parser.rs:263`), they are public fields on a public type — and
`EventStream::next()` reads only `frame.data` (`streaming/event_stream.rs:269`,
`:303`). Nothing in the client consumes them. A caller driving `SseParser`
directly can reach them; nobody using `A2aClient` can. Pairs with (2): the
receive half of resumption is parsed and discarded while the send half is never
emitted.

**5. `AgentCardBuilder`-shaped ergonomics, until the next release.** The
constructors added in this branch live in `a2a-protocol-types` and **cannot be
re-exported from `a2a-protocol-sdk::prelude` in the same release cycle**:
`cargo package` strips path dependencies and resolves `a2a-protocol-types` from
crates.io at `^0.9.0`, which does not contain them. Same mechanism that put
`TaskState::ALL` behind a `#[cfg(test)]` indirection (Addendum 5). See **B1**.

### Deliberately declined

**6. MSRV 1.93 against the official SDK's 1.85.** A decision, recorded as one.
UNVERIFIED this session: I did not re-check `a2a-rs`'s current MSRV.

**7. A scheduler for task retention.** ADR'd in `afe0af9`.

**8. The five gaps §2.1–§2.3 and §2.5 of this document recorded are closed.**
Verified this session, MEASURED:

| §2 finding | Status | Evidence |
|---|---|---|
| 2.1 client does not send `a2a-version` | **fixed** | `client/dispatch.rs:47` `metadata_from` injects `A2A_VERSION_METADATA_KEY`; three tests at `:159`–`:199` |
| 2.1 server does not validate it | **fixed** | `server/methods/tests.rs:66` asserts `a2a-version: 0.3` is rejected, `:78` that `1.0` is accepted |
| 2.2 internal metadata leaks | **fixed** | `server/methods/mod.rs:48` filters `rpc-id` and siblings |
| 2.3 bare `org/ns/app` rejected | **fixed** | `binding.rs:158` `strip_prefix` chain; tests `parses_the_bare_form_the_spec_uses`, `parses_the_slimrpc_scheme` |
| 2.5 binding outside supply-chain gates | **mostly fixed** | `ci.yml` `slimrpc-binding` job now runs its own `cargo-deny` and `cargo package`. Still open: `cargo package --no-verify --allow-dirty` does not build the packaged tarball, and the binding remains outside coverage and mutation (see Q5) |

That is a good record. The previous session's SLIMRPC section was the most
actionable thing in this document and it was acted on.

---

## Q2 — What am I not 100% confident in?

Ranked by blast radius × likelihood. For each: the test that would fail if I am
wrong. I went and got the evidence for the top three; they are marked
**settled**.

### 1. (settled — I was right, and it was worse than I expected) The event queue's `write_timeout` did nothing

**Blast radius:** every streaming agent using the persistent path, which is the
default for `SendStreamingMessage`. **Likelihood before measuring:** high — a
`#[allow(dead_code)]` on a field whose name promises a guarantee.

**The test that would settle it:** fill the persistence channel, then time a
write. MEASURED, probe run 2026-08-19 against `main`:

```
BLOCKED after 1024 writes in 207.935674ms
RESULT: write() STILL blocked after 5s+3s (timeout NOT honoured)
```

`DEFAULT_WRITE_TIMEOUT` is public and 5 seconds. `new_in_memory_queue_with_options`
takes it as a parameter. `EventQueueManager` prints it in `Debug`. It was applied
by nothing. The persistence channel is a bounded `mpsc` (capacity
`max(capacity*16, 1024)`) and `send().await` on a full bounded mpsc waits with no
deadline, so a stalled background processor parked the executor **indefinitely,
with no metric and no trace line**. Fixed in this branch; the two new tests fail
in 2.01s against the pre-fix code and pass after (proof run below, Q6).

Why it survived: the doc comment on `DEFAULT_WRITE_TIMEOUT` read *"Broadcast
sends are non-blocking, so this value is not actively used for backpressure."*
That sentence is true about the broadcast channel and irrelevant to the one that
blocks. The reasoning was applied to the wrong channel.

### 2. (settled — I was wrong about the cause, right that something was wrong) `rate_limit` at 73.62% coverage

**Blast radius:** a rate limiter is a security control; 26% untested would
matter. **The test that would settle it:** the per-file coverage tree, not the
directory rollup. MEASURED,
`curl 'https://api.codecov.io/api/v2/github/tomtom215/repos/a2a-rust/report/tree?depth=6&path=crates/a2a-protocol-server/src/rate_limit'`:

```
100.00%  lines=8    miss=0    config.rs
 96.43%  lines=28   miss=1    identity.rs
 98.70%  lines=154  miss=2    mod.rs
  0.00%  lines=60   miss=60   shared.rs
  0.00%  lines=4    miss=4    unwind_safety.rs
```

64 of the 67 missed lines are two files, and **neither is a testing gap**:
`shared.rs`'s instrumented lines are all `#[cfg(feature = "postgres")]`
`PostgresRateLimitCounter` (three cfg blocks, `shared.rs:51,108,285`) which needs
a live database — exactly the case `codecov.yml` already excludes for five store
files; `unwind_safety.rs` is an `impl std::panic::UnwindSafe` and a `const _:
fn()` compile-time assertion, with nothing to execute. The interceptor's
shared-counter behaviour *is* tested, against stubs, in `rate_limit/shared_tests.rs`.

So "the rate limiter is 73% covered" would have been a false finding. The real
one is smaller and different: **`codecov.yml`'s postgres exclusion list is
incomplete** — `PostgresRateLimitCounter` was added after the list was written.
I did **not** fix it, and that is a deliberate call: the effect of a codecov
ignore is computed server-side and I cannot verify it from here, the change
moves the public badge upward, and shipping an unverifiable change that flatters
the number is exactly what this document exists to catch. Backlog **B6**, with
the evidence and the check that would prove it done.

### 3. (settled — I was wrong, and the correction is the finding) "The book's 130 uncompiled blocks must contain stale API names"

**The test that would settle it:** extract every `use a2a_protocol_*::…`
statement from every Rust block in the book and resolve each imported name
against the crate sources. MEASURED, 2026-08-19:

```
book rust blocks import statements: 95
distinct imported names: 68; unresolved: 0
```

Zero. I also hand-checked five blocks across five chapters
(`client/builder.md`, `building-agents/executor.md`, `stores.md`,
`deployment/production.md`, `error-handling.md`) against the definitions —
`with_protocol_binding`, `TenantAwareInMemoryTaskStore::tenant_count`,
`TenantContext::scope`, every field of `RateLimitConfig` — all present, all
correctly named.

The risk is real and unmeasured *per block*; the sample says it has not
materialised. The honest statement is "130 of 164 Rust blocks in the book are
compiled by nothing, and a name-level check of all of them plus a hand check of
five finds no drift today" — not "the book is rotting".

### 4–8, ranked but not settled this session

4. **The mutation gate's real scope.** `mutants.toml` is 12,669 bytes and
   `cargo-mutants` never reads it (it looks in `.cargo/`). The workflow passes
   its own flags. What would settle it: `cargo mutants --list` with and without
   the file moved to `.cargo/`, diffed. I installed `cargo-mutants` for this and
   then could not run it — see "What I did not verify".
5. **Whether `cargo package --no-verify` in the SLIMRPC job would catch a
   packaging break.** `--no-verify` skips building the tarball. What would
   settle it: delete a file from the binding's `include` list and see whether
   the step still passes.
6. **The 39× SQLite streaming penalty in §3 of this document.** I did not
   re-measure it. It is the largest performance claim in the file and it is now
   UNVERIFIED at this commit.
7. **Whether the official Rust SDK's MSRV is still 1.85.** Carried from
   `docs/rust-sdk-assessment.md`, dated 27 July 2026.
8. **`hot_reload.rs:255`.** `signal(SignalKind::hangup()).expect(…)` runs inside
   a `tokio::spawn`ed task. If registration fails, hot reload dies and the only
   evidence is a `JoinHandle` the caller must poll. Small, real, unfixed (**B8**).

---

## Q3 — Worst developer experience

Built against the **published** 0.9.0 crates, from an empty directory outside
this workspace, following `README.md` literally. Everything below is MEASURED.

```
$ cd /tmp/scratch && cargo new hello-a2a --bin && cd hello-a2a
$ cargo add a2a-protocol-sdk          # 1.8s, locks 92 packages
```

### First five minutes: the README's Quick Start does not compile, and the thing it omits is the hard part

The README's "Implement an agent" and "Start a server" snippets, pasted
verbatim into `src/main.rs`:

```
$ cargo build                                        # 47.3s cold
error[E0425]: cannot find value `agent_card` in this scope
  --> src/main.rs:20:30
   |
20 |             .with_agent_card(agent_card)
```

`agent_card` is used and never constructed. So the new user's first task is to
build an `AgentCard`. The two things anybody tries:

```
AgentCard::new("my-agent", "http://localhost:3000", "1.0.0")
  → error[E0599]: no function or associated item named `new` found for struct `AgentCard`

AgentCard::builder("my-agent", "http://localhost:3000", "1.0.0").build()
  → error[E0599]: no function or associated item named `builder` found for struct `AgentCard`
```

There is no `new`, no `builder`, and no `Default` (OBSERVED,
`crates/a2a-protocol-types/src/agent_card/mod.rs`; the only `impl AgentCard` was
`validate`). At this point the only way forward is to read the struct
definition, because nothing in the error message, the README, or the prelude's
doc comment names the alternative. The working program is a **15-field struct
literal**, and it is 21 of the finished 47-line `main.rs` — 45% of a hello-world
is boilerplate for six `None`s the reader has to look up the types of.

**This is not a small tax and the repository is the proof.** MEASURED:

```
$ grep -rn 'AgentCard {' --include='*.rs' . | wc -l
125
```

125 copies of the same fifteen fields, in this project's own code. The project
paid this more than anyone.

**Fixed in this branch** (`crates/a2a-protocol-types/src/agent_card/builders.rs`):
`AgentCard::new(name, version, interface)` takes exactly the three things
`AgentCard::validate` requires, so a constructed card is valid by construction
and `build()` has nothing to fail at. Everything else is a chained `with_*`, in
the style `AgentCapabilities` already used.
`AgentInterface::jsonrpc/grpc/rest` spell the spec's own binding strings
(`HTTP+JSON` is easy to write as `REST` and a typo there advertises a binding
nothing serves) and default `protocol_version` to `A2A_VERSION`. The
fifteen-field hello-world becomes:

```rust
let agent_card = AgentCard::new("my-agent", "1.0.0",
    AgentInterface::jsonrpc("http://localhost:3000"))
    .with_description("demo")
    .with_input_modes(["text/plain"])
    .with_output_modes(["text/plain"]);
```

A test pins that this serialises **byte-identically** to the literal it replaces
(`a_constructed_card_serialises_identically_to_the_literal_it_replaces`), so
migrating the 125 sites is a refactor and not a wire change.

**The catch, and it is the same one as `TaskState::ALL`:** this cannot reach
`a2a_protocol_sdk::prelude` until `a2a-protocol-types` is published with it.
`cargo package` strips path dependencies and resolves `^0.9.0` from crates.io.
Users get it in 0.10, via `a2a_protocol_sdk::types::AgentCard::new` immediately
(the `pub use a2a_protocol_types::*` glob needs no edit) and via the prelude
once **B1** lands.

### Hour two: the failure that costs a working day

MEASURED, and it happened to me: **`scripts/preflight.sh --full` exhausted this
machine's writable allowance.** It exports CI's environment, and `RUSTFLAGS` and
`CARGO_PROFILE_DEV_DEBUG` are part of cargo's fingerprint, so it keeps a *second*
set of artifacts beside whatever you already built. `target/` reached 29 GB and
`cargo` failed with `No space left on device` partway through gate 17 of ~30.
The script's header warns that this happens and says "`cargo clean` if disk is
tight" — it does not say the number, and it does not check before it starts. A
contributor on a laptop meets this as a mid-run failure in an unrelated tool.
Backlog **B7**: a free-space precheck with the measured figure, which is cheap
and would have saved me an hour.

Second: **compile time**. Cold `cargo build --workspace --all-features
--all-targets` on 4 vCPU is minutes; the `--full` preflight is tens of minutes.
Unchanged from §4.4 of this document, and still the thing a contributor feels
first.

Third, and unchanged: `serve()` versus graceful shutdown (§4.2, fixed in the
previous branch), no CLI (§4.3), and ten feature flags on the umbrella crate
with SLIMRPC unreachable from any of them (§4.5).

---

## Q4 — Worst performance

### The realistic worst path: push-notification fan-out, and it is not in the benchmark suite

**MEASURED**, from a test written for this review
(`configs_the_deadline_never_reaches_are_counted_as_skipped`), run at the
**shipped defaults** — `max_push_configs_per_task = 100`,
`push_delivery_timeout = 5s`, per-event budget 30s (hardcoded,
`push_delivery/mod.rs`):

```
delivered = 6      skipped = 94
```

Six of a hundred configured webhooks receive each event. The other 94 are not
contacted at all. The arithmetic is exactly:

```
configs_reached == min(len, budget / push_delivery_timeout)
```

Three things make this the worst realistic path rather than a contrived one:

1. **Delivery is sequential.** OBSERVED, `push_delivery/mod.rs`: a `for` loop
   with an `await` per config. A `tokio::sync::Semaphore::new(16)` was
   constructed per event under a comment describing it as a cap on "hundreds of
   concurrent HTTP requests" — a mitigation for a shape this loop has never had.
   Its own neighbouring comment admitted "deliveries are currently sequential".
   Removed in this branch.
2. **The shortfall was invisible.** The only report was one `trace_warn!`, and
   `trace_warn!` expands to nothing without the non-default `tracing` feature.
   In the build most people run, 94 webhooks going unnotified produced *zero*
   observable output. Fixed: each skipped config now increments
   `push_outcome::SKIPPED`.
3. **It back-pressures onto the executor.** `deliver_push_bg` is awaited inline
   in the per-task background processor, which is also what drains the
   persistence channel. A slow webhook estate therefore stops task persistence
   for that task — and until this branch, once the 1,024-deep channel filled,
   `EventQueueWriter::write` blocked **forever** (Q2.1).

Blast radius is per task, not per process: the processor is spawned per task
(OBSERVED, `event_processing/background/mod.rs`). That is the right design and it
is why this is a bad day rather than an outage.

### Where the benchmark harness measures the wrong thing

**MEASURED**: the suite has 15 bench files and 77 benchmark groups. Two are named
for push notifications — `enterprise/push_config` and `production/push_config`.
Both measure **config CRUD**: `bench_push_config_roundtrip`
(`benches/benches/production_scenarios.rs:363`) times
`client → server → store → response` for `set_push_config`.

**Nothing in the suite times a push *delivery*.** The one push-notification path
with a 30-second budget, a hardcoded fan-out ceiling and an outward HTTP call is
unmeasured, while the CRUD call that writes a row is measured twice. That is the
harness measuring the wrong thing, precisely.

Second, smaller instance: `backpressure/*` measures queue *volume*
(`sqlite_store_502_events`) but not queue *saturation* — no benchmark or test
filled the persistence channel before this branch, which is why the unbounded
block survived.

### Re-stating §3 honestly

§3 of this document reports 170–540 µs per persisted streaming event on SQLite
and a 39× penalty against memory. **I did not re-measure it**, and it is
therefore UNVERIFIED at `4ddb7ab`. It remains the largest performance claim in
this file. Re-running its probe is backlog **B9**.

---

## Q5 — What gaps do we have?

### Coverage, and the files the tooling cannot see

MEASURED, `curl https://api.codecov.io/api/v2/github/tomtom215/repos/a2a-rust/`
(report of 2026-08-18T18:47Z): **94.06%**, 35,287 lines, 33,191 hits, 2,096
misses.

MEASURED, the depth-1 tree: it contains exactly one entry, `crates`. Which means:

* **`bindings/a2a-protocol-slimrpc` — 3,346 lines of a `publish = true` crate —
  has no coverage measurement at all.** It is not in `codecov.yml`'s `ignore`
  list either. It is absent because `cargo llvm-cov --workspace`
  (`coverage.yml:54`) cannot see a crate outside the workspace. That is worse
  than an exclusion: an exclusion is a recorded decision, and this is a blind
  spot nothing records.
* The same is true of `benches/`, `book-tests/` and `fuzz/`, which matters less.

Directory rollups, worst first (MEASURED, depth-3 tree):

| path | coverage | lines | missed |
|---|---:|---:|---:|
| `server/src/rate_limit` | 73.62% | 254 | 67 |
| `server/src/store` | 83.64% | 4,082 | 668 |
| `server/src/push` | 85.64% | 2,180 | 313 |
| `client/src/token_provider.rs` | 92.39% | 657 | 50 |
| `client/src/transport` | 93.78% | 3,197 | 199 |
| `server/src/handler` | 93.83% | 5,369 | 331 |

`rate_limit` is an artifact — see Q2.2; the real number for code that can be
executed is ~98%. `store` at 83.64% is the largest *real* pool of untested
lines in the tree (668), and it is the subsystem the retention work touched
last. `push` at 85.64% is the second, and Q4 is a demonstration that the
untested part of it is not cosmetic.

### Test gaps

MEASURED, by a scanner over all 2,842 `#[test]` / `#[tokio::test]` functions in
tracked `*.rs`: **32 contain no assertion of any kind** — no `assert*`, no
`unwrap`, no `expect`, no `?`, no `should_panic`. That is 1.1%, and most are
legitimate: `crates/a2a-protocol-sdk/tests/reexport_tests.rs` asserts by
*importing* (the compile is the test), and `a2a_error_is_std_error` asserts by
coercing to `dyn Error`.

Two were not legitimate, and both are fixed in this branch. See Q6.

### Protocol gaps

None found against A2A v1.0 that the SPEC_COMPLIANCE matrix does not already
track. The open conformance items are recorded in
`docs/official-tck-findings.md` §5 and §17 and I did not re-run the official
TCK (see "What I did not verify"). Q1(2) — SSE `id:`/`Last-Event-ID` — is not a
spec gap; the spec is silent on it.

### Operational gaps

Covered under Q7. The load-bearing one is that until this branch there was no
telemetry for either of the two ways a streaming agent quietly stops making
progress: a full persistence channel and an unreachable webhook estate.

---

## Q6 — What misses the engineering-excellence bar?

### A gate that was red on `main`, on a commit CI never ran

This is the most serious finding in this addendum, and it is a process defect
rather than a code one. MEASURED, in this order:

```
$ git log --oneline -1 main
4ddb7ab chore: update benchmark results

$ ./scripts/check_benchmark_prose.sh ; echo $?
check_benchmark_prose: benchmarks.md prose disagrees with its own tables
  DRIFT  prose does not contain '22.8%' (64B->16KB latency increase)
  DRIFT  prose does not contain '128.7 µs (39.8%)' (connection-reuse saving)
1

$ grep -n check_benchmark_prose .github/workflows/ci.yml
100:        run: ./scripts/check_benchmark_prose.sh
```

and the run list for `ci.yml` on `main` (GitHub Actions API): the newest run is
`6c926a77`, the PR #111 merge. **There is no CI run for `4ddb7ab`.**

The cause, OBSERVED: `benchmarks.yml:57` checks out with
`token: ${{ secrets.GITHUB_TOKEN }}` and `:139` pushes to `main`. GitHub does
not trigger workflow runs for pushes authenticated with `GITHUB_TOKEN` — by
design, to stop recursion. So **every** "chore: update benchmark results"
commit lands on `main` without CI, and the one job that rewrites
`benchmarks.md` is the one job that never checks it.

This is the same shape as Addendum 5's `dco.yml` finding — *a gate whose trigger
does not cover the event that breaks it* — and it is the second instance. That
makes it a class, not an accident.

**And the gate could not be satisfied by its own remedy.** Its failure message
says "Regenerate it with `cargo bench` + `generate_book_page.sh`". Regenerating
would not have fixed it, because (MEASURED, by reproducing both derivations):

```
$ python3 -c "print(f'{((241.4-196.5)/196.5)*100:.1f}%')"   # from the rounded table
22.8%
$ python3 -c "print(f'{(323.0-194.3):.1f} µs')"             # from the rounded table
128.7 µs
```

The generator computes the prose from full-precision `estimates.json`
(`extract_median_ns`) and got 22.9% and 128.6 µs. The checker recomputes from
the **rounded table on the page** and gets 22.8% and 128.7 µs. Both are
arithmetically right; they differ only by the rounding the table itself
introduces. A regenerate reproduces the full-precision answer every time, so the
gate stays red on correct output.

Fixed in this branch, three ways, because each addresses a different half:

1. `generate_book_page.sh` gains `extract_median_ns_as_tabled`, which quantises
   to exactly the precision `extract_median` prints, and `derive_pct_increase` /
   `derive_saving` now use it. Prose and tables agree **by construction**, which
   is the property the checker is entitled to assume.
2. The three stale prose figures are corrected by hand to the values the rounded
   tables imply — the same values the fixed generator will now produce. Editing
   this page by hand is normally the wrong move; here the correct value is
   *defined by* the table, and the alternative was leaving `main` red until the
   next bench run. `./scripts/check_benchmark_prose.sh` now exits 0.
3. `benchmarks.yml` runs `./scripts/check_benchmark_prose.sh` after generating
   and before committing, so the job that writes the file is the job that stops
   a bad one reaching `main`. Registered in
   `scripts/prove_workflow_gates_fail.py` as `Exempt` — the script is already
   proven able to fail by `scripts/prove_gates_fail.sh:312` from its `ci.yml`
   call site, and two harnesses claiming one gate is what that file's own header
   rules out. Without registering it the step would have been **invisible** to
   the prover: its body is a bare script call with no literal `exit`, so
   `EXPLICIT_FAIL` does not match and it is not auto-selected. Prover now reports
   `20 proven, 0 unproven, 14 exempt, 0 not selected (of 34 registered)`.

### A documentation gate that runs in exactly one configuration

MEASURED: `ci.yml:355` runs `cargo doc --workspace --no-deps` with
`RUSTDOCFLAGS: -D warnings`, and it passes. Per-crate, at default features:

```
$ cargo doc --no-deps -p a2a-protocol-server
warning: unresolved link to `crate::auth::jwt::JwtAuthInterceptor`
warning: unresolved link to `PostgresRateLimitCounter`   (x2)
warning: unresolved link to `A2aError`
warning: `a2a-protocol-server` (lib doc) generated 4 warnings
```

Four broken links that CI cannot see, because `--workspace` unifies features
across members and something else in the workspace turns on `auth-jwt` and
`postgres`. A user running `cargo doc -p a2a-protocol-server` sees all four.
(docs.rs is unaffected — all four manifests set `all-features = true`.) Fixed:
the feature-gated references became plain code spans naming the feature they
need, and `A2aError` got a fully-qualified path. All four crates are now at zero
warnings in both configurations.

### Two tests that asserted nothing while their names asserted something

1. `bom_in_first_line_buf_stripped_by_process_line`
   (`client/src/streaming/sse_parser/parser.rs`) — **47 lines, every one a
   comment**, ending in "Skip this test". Its body constructed a parser, bound
   it to `_p`, and stopped. Its name claimed a fragmented BOM is stripped by
   `process_line`, which is a real, load-bearing claim. Its reasoning was also
   out of date: it argued from a feed-time BOM fast-path that has since been
   removed, so its conclusion ("lines 165–168 are dead code in practice")
   described code that no longer exists — `process_line` is now the *only* place
   a BOM is stripped. **Deleted**, with a note pointing at
   `bom_split_one_byte_at_a_time_first_event_survives` and
   `bom_split_two_then_one_byte_first_event_survives`, which cover the same
   behaviour with assertions.
2. `build_sse_response_client_disconnect_stops_stream` (`server/src/streaming/sse.rs`)
   — dropped the response, slept 50 ms, wrote one event with `let _ =` under a
   comment reading "may or may not succeed depending on timing", and ended. It
   could not fail, while its name claimed the writer loop stops. **Rewritten**
   to assert the observable consequence: the loop only learns the client is gone
   when a send fails, so the *first* write teaches it and the *second* finds the
   reader dropped. Polls rather than sleeping a fixed amount, because the exit
   is the thing under test.

### The 500-line exemption list: is it actually shrinking?

MEASURED, entry counts from git history:

| date | commit | entries |
|---|---|---:|
| 2026-08-10 | `6d1521e` | 77 |
| 2026-08-11 | `c03a510` | 79 |
| 2026-08-17 | `567364c` | 80 |
| 2026-08-18 | `cdad9ad` | 83 |
| 2026-08-19 | this branch | **82** |

Raw, it grew 77 → 83 in nine days, against a file whose own first line says
"recorded so the list can only shrink". But the raw number conflates three
causes, and diffing the entries shows it (MEASURED, `diff` of the two lists):

* **+6 from a scope widening**, not from new long files: `SCOPE` gained `*.sh`
  and `*.py` on 2026-08-11 and four scripts were already over the limit.
* **+3 from splits that improved the tree**: `messaging.rs`, `sqlite_store.rs`
  and `rate_limit.rs` each became two files, and two of the two are still over
  500 — so a genuine improvement *raised* the count.
* **−2 from real shrinkage**: `echo-agent/src/main.rs` and
  `incident-response/src/main.rs` came off.
* **+2 genuinely new files born over the limit and exempted rather than split**:
  `tests/multi_replica.rs` (531) and `tests/soak_multi_replica.rs` (563), both
  added 2026-08-17.

So the honest verdict is **two**, not six, and the baseline records a reason for
both. That is a ratchet working roughly as designed, and "the list is growing"
would have been a bad finding.

Two real weaknesses remain, and neither is the count:

* **`--update` deletes every comment in the file.** The baseline says so itself:
  "Regenerating with --update drops every comment above. If you run it, put them
  back." A ratchet whose maintenance command destroys its own justifications is
  one `--update` away from being an unexplained list. (I removed this branch's
  one stale entry by hand for exactly this reason.)
* **Growth of a listed file is unbounded.** Deliberate, and documented — but it
  means `websocket.rs` at 1,871 lines is as legal as one at 501.

This branch's own contribution: `agent_card.rs` (523) became
`agent_card/{mod.rs 301, builders.rs 434, tests.rs 237, builders/tests.rs 238}`,
removing an entry; and `push_delivery.rs` crossed 500 when its new test landed
and was **split rather than exempted** (`mod.rs` 136, `tests.rs` 405).

### `#[allow]` without a justification comment

MEASURED: 156 `#[allow(...)]` / `#![allow(...)]` in library sources; **91 have
no comment on the line above**. That is measured against a bar CONTRIBUTING.md
does not actually set — it requires justification for `expect()`, not for
`#[allow]` — so 91 is not 91 defects. Most are mechanical:
`clippy::manual_async_fn` appears ~20 times because the object-safe async traits
require it, and `used_underscore_binding` is explained once in `trace.rs`'s
module docs rather than at each site.

**One of the 156 was hiding a real defect**, which is the argument for caring at
all: `#[allow(dead_code)] write_timeout` on `InMemoryQueueWriter`, with the
comment "Retained for API compatibility". Q2.1 is what that was retaining.

### `unwrap()` / `expect()` in library code: the recorded number has decayed

CONTRIBUTING.md records **26 in 14 files**, measured 2026-08-10, by "excluding
in-file `#[cfg(test)]` modules". That method stopped working when test modules
moved into their own files (`messaging/tests.rs`, `sqlite_store/tests.rs`,
`rate_limit/tests.rs`, `error/tests.rs`, `auth/identity_tests.rs`, …): a scanner
that strips in-file `cfg(test)` blocks now reports **361**, because it counts
whole test files as library code.

MEASURED with a scanner that also excludes files reached through
`#[cfg(test)] mod x;`: **14 in 6 files** (15 before this branch removed the
`semaphore.acquire().expect(…)`). The number improved; the *documented method
for computing it* broke. That is the failure CONTRIBUTING's own file-length
header names — "a number nothing recomputes is a number that decays" — applied
to CONTRIBUTING. Backlog **B5**: make it a script, like every other number in
this repository that matters.

---

## Q7 — What misses the production-ready bar?

Judged against what an operator needs at 3am. Each miss is labelled **defect**,
**documentation gap** or **scope boundary**, because the previous session was
right that the difference is usually the finding.

### Failure visibility

**Defect, fixed.** Two of the three ways a streaming agent silently stops making
progress had no telemetry at all:

| symptom at 3am | signal before this branch | after |
|---|---|---|
| agent stopped emitting; task stuck in `working` | none — `write()` blocked forever, no metric, no trace | error to the executor after `write_timeout`, naming the cause |
| webhooks not firing for 94 of 100 configs | one `trace_warn!`, compiled out in a default build | `push_delivery{outcome="skipped"}` per config |
| store rejecting writes | `on_persistence_error` | unchanged (added in a previous session) |

**Documentation gap, open.** The `tracing` feature is **not** default on any of
the four crates (OBSERVED, each `[features]` block). Every `trace_*!` in the
codebase compiles to nothing unless an operator opts in. That is a defensible
default for a library — and it means the deployment guidance has to say
"turn on `tracing` and `otel` or you are running blind", in those words. The
book's *Production Hardening* chapter does not (OBSERVED,
`book/src/deployment/production.md`). Backlog **B10**.

### Backpressure

**Defect, fixed** — Q2.1. The one queue in the system that can block was
unbounded in time. It is now bounded by the parameter that already existed for
the purpose.

**Scope boundary, undocumented.** The remaining ceiling is the arithmetic in
Q4: a task whose webhooks are slow processes events at `budget /
push_delivery_timeout` per event, and once the 1,024-deep persistence channel
fills, further writes now fail the task rather than hanging it. Failing is the
right outcome and it needs to be a documented one.

### Resource ceilings

**Adequate.** `HandlerLimits` carries twelve of them with shipped defaults
(OBSERVED, `handler/limits.rs`): id length, metadata size, cancellation tokens
and their max age, artifacts per task, parts per artifact, context locks, push
configs per task and in total, subscribe reattach interval and max idle. The
store has a capacity bound and amortised eviction. `ServeConfig` bounds
connections, header-read time, idle time and drain.

**One is not there:** the 30-second per-event push budget is a hardcoded
`Duration::from_secs(30)` in `push_delivery/mod.rs`, not a `HandlerLimits`
field. An operator with 100 configs cannot raise it, and it is the term that
decides how many of their webhooks get called. **Defect (small)**, backlog
**B3**; not fixed here because adding a public field to a non-`#[non_exhaustive]`
`HandlerLimits` is a breaking change for struct-literal constructors and belongs
in a release-planned batch.

### Upgrade and rollback, data migration

**Adequate, and unusually well done.** `pg_migration.rs`, SQLite migrations, a
`semver` gate that has been blocking since 0.7.0, `RELEASING.md`, and — new
since 0.9.0 — an opt-in `purge_expired` with a `PurgeReport` rather than a timer
that fires during your traffic peak. UNVERIFIED this session: I did not run the
live-PostgreSQL suite (26 `--ignored` tests) or the migration path.

### Secret handling

**Adequate on the evidence I checked.** `deny.toml`, `run_secret_scanning` in
CI, `SECURITY.md`, and the auth interceptors take providers rather than literals.
UNVERIFIED: I did not audit the SLIMRPC binding's SPIFFE credential handling
beyond noting that `spiffe_rotation.rs` and `spiffe_federation.rs` exist.

### Multi-tenancy isolation

**Documentation gap.** The implementation is real and tested —
`TenantAwareInMemoryTaskStore`, `TenantContext::scope`, tenant-aware SQLite and
Postgres stores and push-config stores, `tenant_resolver.rs`, and
`AgentInterface::tenant` on the wire. MEASURED: "multi-tenan" appears in 19 book
files. But there is **no chapter on it** (MEASURED, `grep -ic multi-tenan
book/src/SUMMARY.md` → 0), so an operator evaluating tenancy has to assemble the
story from nineteen places. Backlog **B10**.

### The one I would not ship without

Nothing, now. Before this branch it would have been Q2.1: an SDK where a slow
webhook can park an agent forever with no telemetry is not a 3am-safe SDK, and
the knob that would have bounded it was `#[allow(dead_code)]`.

---

## Q8 — Examples still to build

### Do they run? Are they in CI?

MEASURED, `grep -rln -- "-p <crate>" .github/workflows/`:

| example | src LOC | executed by a workflow | `#[test]`s |
|---|---:|---|---:|
| agent-team | 8,911 | `ci.yml` (dogfood, `--all-features`) | 2 |
| incident-response | 3,498 | `ci.yml` (twice: surface + `harden`) | 0 |
| harness | 920 | — (library) | 4 |
| echo-agent | 692 | `ci.yml`, `tck.yml` | 2 |
| multi-lang-team | 679 | `ci.yml` | 0 |
| rig-agent | 647 | `ci.yml` | 0 |
| genai-agent | 632 | `ci.yml` | 0 |
| deploy-agent | 416 | **no workflow** | 5 |
| hello-agent | 142 | **no workflow** | 3 |

**`hello-agent` — the README's own first example, the first thing any evaluator
runs — is executed by no workflow.** It compiles under `cargo build --workspace`
and its three `#[test]`s run under `cargo test --workspace`, so it is not
unverified; the binary is never started. `deploy-agent`, whose entire subject is
deployment, is in the same position. Backlog **B2**, and it is cheap: both are
short-lived processes.

### Which end of the 142 ↔ 8,911 range is wrong?

**The 8,911 end.** `hello-agent` at 142 lines is the right size for what it is,
and it will get shorter when the constructors from this branch reach the SDK
(the `AgentCard` literal is a fifth of it). `agent-team` at 8,911 lines is not
an example — it is a 100-test end-to-end conformance suite that happens to live
under `examples/`, and `ci.yml` treats it that way, running it as the `dogfood`
job with three distinct exit codes. Calling it an example is the problem: a
reader who opens `examples/` looking for something to copy meets it first by
size.

**Recommendation, with its cost:** move `agent-team` out of `examples/` to
`dogfood/` (or `tests/e2e/`) and leave a one-paragraph pointer. Cost: touching
`ci.yml`, the package-exclude list in the `package` job, `codecov.yml`'s
`examples/**` ignore, `book/src/examples/agent-team.md` and `SUMMARY.md`, plus
the churn of a large `git mv` in review. I did **not** do it in this branch —
it is a rename with a wide blast radius and no test can tell you it worked, so
it wants to be its own PR, not a rider on eight other changes.

### The three specifics the brief named

* **`multi-lang-team` has no Rust worker.** Confirmed OBSERVED; unchanged. The
  example whose subject is polyglot interop demonstrates every language *except*
  the one the SDK is written in. Backlog **B11**.
* **No example uses SLIMRPC.** Confirmed. `bindings/a2a-protocol-slimrpc` has
  37 integration test functions across ten test files and a `slim-node` binary, and zero
  examples. For a binding this project wants to publish, that is the gap
  between "tested" and "adoptable". Backlog **B11**.
* **`echo-agent` vs `incident-response` overlap.** Not actually unclear once
  measured: `ci.yml`'s `example-surface` job runs both because they exercise
  *different* things — echo-agent drives every method over every binding, and
  `incident-response` is the one that ends a stream **without** a terminal task
  state (an agent asking a clarifying question), which is the case that found a
  real WebSocket client defect. That distinction is in a CI comment
  (`ci.yml`, `example-surface`) and nowhere a reader of `examples/README` would
  find it. **Documentation gap**, not duplication.

---

## Q9 — Documentation and GitHub Pages

### The site builds and renders

MEASURED, 2026-08-19:

```
$ curl -sS -o /dev/null -w '%{http_code} %{size_download}\n' -L <url>
https://a2a-rust.com/                                    200  35762
https://a2a-rust.com/reference/api-reference.html        200  48233
https://a2a-rust.com/api/a2a_protocol_types/index.html   200  28607
https://tomtom215.github.io/a2a-rust/                    200  35762
```

Pages is live, the custom domain and the `github.io` origin both serve, rustdoc
is published under `/api/` and is reachable. `docs.yml:76–80` fails the deploy
if any crate's rustdoc index is missing, so an empty `/api/` cannot ship.

### The gates that guard it both pass

```
$ python3 scripts/check_api_reference.py
check_api_reference: 116 name(s) listed in api-reference.md
OK — every name on the page is defined in the crates.

$ ./scripts/check_book_code.sh
check_book_code: 130 ignored block(s) across 21 file(s), matching the baseline;
every other Rust block in the book is compiled
```

**The API Quick Reference is not decaying** in the direction its checker looks —
and the checker is explicit that it is one-directional (it catches names on the
page that no longer exist, not public items missing from the page). That is a
defensible design, stated in the script's own header, and it means "not
decaying" is a narrower claim than it sounds. I did not attempt the reverse
check.

### The number that deserves to be on the record

MEASURED, by classifying every fenced Rust block in `book/src`:

| fence | count | compiled? | executed? |
|---|---:|---|---|
| ` ```rust,ignore ` | **130** | no | no |
| ` ```rust,no_run ` | 31 | yes | no |
| ` ```rust ` | 3 | yes | yes |
| **total** | **164** | 34 (21%) | 3 (2%) |

**79% of the book's Rust is compiled by nothing.** `check_book_code.sh` is a
shrink-only ratchet on that number, added 2026-08-16 — so "not shrinking" is not
yet a fair criticism (it has existed for three days and has gone 130 → 133 →
130). The concentration is where a new reader starts:
`client/builder.md` 12, `building-agents/executor.md` 12,
`building-agents/dispatchers.md` 10, `client/error-handling.md` 10,
`deployment/production.md` 9, `building-agents/stores.md` 9.

Q2.3 records what happened when I went looking for the rot this implies: a
name-level check of all 95 import statements in the book's Rust blocks resolved
**68 of 68** distinct names, and a hand check of five blocks found nothing
stale. The risk is real; today it has not bitten.

### What is missing

**No chapter on troubleshooting, multi-tenancy, observability, security or
monitoring.** MEASURED: none of the five terms appears in
`book/src/SUMMARY.md`. The material is not absent — 19 files mention
multi-tenancy, 16 mention security, 7 observability — it is *unassembled*.
There are 45 chapters and none of them is the page an operator opens at 3am.

Of the five, **troubleshooting is the one with no home at all**: zero book files
match it. The other four have adjacent chapters (*Production Hardening*,
*Authentication*, *Running More Than One Replica*) that a chapter could be
lifted out of. Backlog **B10**.

---

## Q10 — The SLIMRPC transport, and whether to publish it

### Defining "complete" against `spec/slimrpc_v1/`

MEASURED, `./scripts/check_slimrpc_spec.sh` → `2 vendored file(s) match
upstream`. The vendored spec is current for the two files it tracks.

| requirement | state | evidence |
|---|---|---|
| 11 A2A methods | **complete** | `src/server/methods/mod.rs` dispatches 11; `Method::ALL` is 11; `check_method_denominator.py` cross-checks the proto |
| Multicast | **complete** | `src/multicast/`, `tests/multicast.rs`, `spec/slimrpc_v1/slimrpc-multicast.md` vendored and matching |
| Service parameters (§3 MUST) | **complete** — was the previous session's §2.1 | `client/dispatch.rs:47` injects `a2a-version`; server rejects `0.3` (`server/methods/tests.rs:66`) |
| Internal-metadata filtering | **complete** — was §2.2 | `server/methods/mod.rs:48` |
| Address forms (`org/ns/app`, `slim://`, `slimrpc://`) | **complete** — was §2.3 | `binding.rs:158`, four parse tests |
| SPIFFE / mTLS | **complete** | `tests/spiffe.rs`, `remote_node_mtls.rs`, and a CI job that installs real SPIRE |
| Federation | **complete** | `tests/spiffe_federation.rs` |
| Credential rotation | **complete** | `tests/spiffe_rotation.rs` |
| Error mapping | **complete** | `src/error.rs`, `rpc_error_to_client_error`, deadline → `Timeout` |
| Out-of-process / multi-hop | **complete** | `tests/out_of_process.rs`, `tests/remote_node_multihop.rs`, `src/bin/slim_node.rs` |
| Backpressure | **not specified, not implemented** | neither the vendored spec nor the binding says anything about it; see below |

That is a genuinely complete transport by the vendored spec's own terms. 3,346
source lines, 37 integration test functions across ten test files (plus 34 unit tests in `src`), real SPIRE in
CI. Depth is not the problem.

### The delta that remains

1. **Backpressure is unaddressed on both sides.** The spec is silent, and the
   binding inherits whatever `agntcy-slim-rpc` does. This is the one row above
   that is neither "done" nor "declined" — it is *unexamined*, and Q2.1 is what
   unexamined queueing looked like in the part of the codebase that was
   examined. UNVERIFIED and it is the first thing I would look at.
2. **`cargo package --no-verify --allow-dirty`** (`ci.yml`, `slimrpc-binding`)
   validates the manifest and file list but does not build the tarball. The
   workspace's own `package` job does verify. A packaging break that only shows
   up on a clean build from the tarball would pass.
3. **Zero coverage and zero mutation measurement** — Q5. Not excluded; invisible.
4. **`check_slimrpc_spec.sh` compares two named files** and cannot see a *new*
   file appearing upstream. Recorded in §2.4 and still true.

### Should it be published to crates.io? — Recommendation: not yet, and here is the one thing that decides it

It is `version = "0.2.0"`, `publish = true`, and has never been released
(MEASURED: crates.io returns `crate 'a2a-protocol-slimrpc' does not exist`).

**Recommendation: hold, and the blocker is the coverage/mutation blind spot, not
the code.** Publishing puts a name on crates.io that this project then owes
support for. Everything else in the four-crate set is measured by two
independent harnesses that can each catch what the other misses; this one is
measured by its own test suite and nothing else. That suite is good — better
than the official crate's four test functions in one topology — but "good suite,
no second opinion" is precisely the position `agent-team` was in before anyone
measured that CI had never executed it.

Concretely, what I would want before pushing the button, in order:

1. **Coverage for the binding.** `cargo llvm-cov` in the `slimrpc-binding` job,
   uploaded under a distinct Codecov flag. Cheap; the job already builds and
   tests it. *(B12)*
2. **Drop `--no-verify`** from its `cargo package` step, or say in the workflow
   why it cannot be dropped. *(B12)*
3. **One example.** Nothing in `examples/` uses SLIMRPC. Publishing a transport
   nobody has written a hello-world against is publishing an untested onboarding
   path. *(B11)*
4. **A version-coupling statement in the manifest**, not only in the book. The
   binding is `0.2` against four `0.9` crates; a user who reads only crates.io
   sees no rule at all.

The counter-argument, and it is real: `agntcy-slim-rpc` moves, and an unpublished
binding cannot accumulate the downstream signal that would tell you when it
breaks. If the maintainer's priority is finding out whether anyone wants this,
publishing `0.2.0` as an explicit preview — with the README saying so — costs
little and buys information. I would still do (1) and (2) first; they are hours,
not days.

---

## Q11 — What should be done differently

### My own working method, and where it failed today

Addendum 5 diagnosed the previous session's three CI failures as one class:
*checking two points on an eleven-point matrix while a purpose-built preflight
script sat unused*. I read that before starting, and **I reproduced it anyway**,
in a smaller way, and it is worth recording exactly how.

I changed `push_delivery.rs`, ran `cargo clippy -p a2a-protocol-server
--all-targets --all-features`, got a clean result, and moved on. The full
preflight then failed two gates:

```
error: used underscore-prefixed binding
  --> .../push_delivery/mod.rs:101:22
error: this function has too many lines (74/60)
  --> .../push_delivery/tests.rs:215:7
```

The first only appears in a build **without** the `tracing` feature, because
that is the build where `_delivered`'s only other reader disappears and my new
use of it becomes the sole one. `--all-features` cannot see it. This is the
same mechanism as before — a feature-conditional lint — and knowing about the
mechanism did not stop me, because I still ran the one command that felt
sufficient rather than the one that copies CI.

The lesson is not "remember the matrix". It is **run `scripts/preflight.sh`
before you believe anything, including when the change looks local**. The script
exists, it reads its gate list from `ci.yml` so it cannot drift, and it costs
wall-clock rather than attention. I have used it for every subsequent change in
this branch.

Second, smaller, and also mine: `preflight --full` filled the disk (Q3), which
cost about an hour of re-running from a clean `target/`. That one is the tool's
to fix (**B7**), but the working-method half is: **run the expensive check
first, on a clean tree, before you have edits to lose.**

### Process

**1. Close the `GITHUB_TOKEN` push loophole as a class, not as an instance.**
Q6 is the second gate found whose trigger does not cover the event that breaks
it (`dco.yml` was the first). Both were found by a human reading trigger blocks.
`scripts/prove_workflow_gates_fail.py` proves gates can *fail*; nothing proves
gates *run on the commits that can break them*. A checker that reads each
workflow's `on:` block, and each `git push` in the repository's own workflows,
and asserts that every gate-bearing job is reachable from every event that can
change its inputs, would have caught both. That is the highest-leverage new gate
I can name, and it is real work — a day, not an hour. **B13.**

**2. Prefer "make the numbers agree by construction" to "check that the numbers
agree".** The benchmark-prose gate was correct and unsatisfiable at the same
time, because two components computed the same quantity from different
precisions. The fix that stuck was making the generator quantise, not making the
checker lenient. Where this repository has a checker and a generator for the
same fact, the generator should consume exactly what the checker will read back.

**3. Say what a documented measurement's *method* is, in a script.**
CONTRIBUTING's "26 unwrap/expect" is now unreproducible by its own description
(Q6). Every other load-bearing number here — file lengths, book ignores,
benchmark prose, OTel coverage, method denominator — has a script. That one does
not, and it is the one that decayed. **B5.**

### Architecture

**4. Delete scaffolding for shapes the code does not have.** The
`Semaphore::new(16)` in `push_delivery` and the `#[allow(dead_code)]
write_timeout` were both future-proofing. Both read, to anyone auditing, as
protections that existed. One of them was covering a defect that took eight
seconds to demonstrate. A comment saying "we do not bound this, here is the
arithmetic that decides it" is worth more than a semaphore that bounds nothing.

**5. Publish primitives with their integration, or say loudly that you are
not.** `sign_agent_card`/`verify_agent_card` are good code with good tests, and
two crates advertise features named for them that wire up nothing. The
consequence is a user who turns on `signing` and believes their cards are being
verified.

### Priority

If I had to pick three things for the next release, in order:

1. **`AgentCard::new` reaching the prelude** (**B1**). It is written and tested;
   it needs the release that makes it reachable. Nothing else in the backlog
   changes what a new user meets in their first five minutes.
2. **The trigger-coverage gate** (**B13**). Two instances is a class.
3. **A troubleshooting chapter and an observability chapter** (**B10**). 45
   chapters and none of them is the page an operator opens at 3am — and this
   branch just added two new signals (`SKIPPED`, the queue-full error) that
   nothing tells anyone to look for.

What I would **not** prioritise: the SQLite artifact-normalisation schema change
from §3. It is the biggest performance win available and it is a schema
migration for a store with, on the evidence of Q"downloads", very few
deployments to migrate. Do it when there is someone to do it for.

---

## What this branch changed

Eight fixes, each with tests, each run through `scripts/preflight.sh --full`.
Highest value first.

### 1. The event queue's `write_timeout` is applied — `streaming/event_queue/`

The defect and its measurement are Q2.1. `persistence_tx.send(…).await` became
`send_timeout(…, self.write_timeout)`; the `#[allow(dead_code)]` is gone; a full
channel returns an error naming the cause, a *closed* channel stays non-fatal
(the processor is gone, live subscribers can still be served, and the caller is
not producing state that is being silently dropped).

`DEFAULT_WRITE_TIMEOUT`'s doc comment previously reasoned about the broadcast
channel — the one that cannot block — and concluded the value was unused. It now
says what it bounds and why the old reasoning was applied to the wrong channel.

**Three tests**, and the first two are proven able to fail. Reverting the
`send_timeout` and re-running:

```
test ...::a_full_persistence_channel_fails_the_write_within_the_timeout ... FAILED
test ...::the_failing_write_waits_about_one_write_timeout ... FAILED
test result: FAILED. 18 passed; 2 failed; finished in 2.01s
```

They fail in 2.01s rather than hanging, deliberately: each write is wrapped in
an outer `tokio::time::timeout` an order of magnitude past the deadline, because
the pre-fix behaviour is an *infinite block* and a test that hangs burns the
whole CI job instead of naming the defect. (My first draft did hang; a 120s
`timeout` around `cargo test` is how I found out.)

### 2. Push-notification shortfall is counted — `handler/event_processing/background/push_delivery/`

Q4. New `push_outcome::SKIPPED`, one count per config the budget never reaches.
The dead `Semaphore::new(16)` and its comment are removed, replaced by the
arithmetic that actually governs the loop.

Proven able to fail — deleting the counting loop:

```
assertion `left == right` failed: every config must be accounted for: 6 delivered, 0 skipped
  left: 6   right: 100
```

The test runs on paused time (`#[tokio::test(start_paused = true)]`), so a
30-second budget costs 0.05s of wall clock. Addendum 4's rule is that paused
time and a *real socket* do not mix; there is no socket here, only `sleep`.

### 3. The benchmark-prose gate is satisfiable, and runs where it is needed

Q6, three parts: `extract_median_ns_as_tabled` in the generator, three corrected
prose figures, and the gate added to `benchmarks.yml` before its commit step and
registered in `prove_workflow_gates_fail.py`. `./scripts/check_benchmark_prose.sh`
now reports `12 quoted value(s) agree with the tables on the page`.

### 4. `AgentCard::new` and friends — `a2a-protocol-types/src/agent_card/builders.rs`

Q3. Constructors for `AgentCard`, `AgentInterface` and `AgentSkill`, in the
crate's existing `X::none()` + chained-`with_*` style. Every method carries a
doc example (18 new doctests, all passing); a test pins byte-identical
serialisation against the literal it replaces.

`agent_card.rs` (523 lines) became `agent_card/{mod.rs, builders.rs, tests.rs,
builders/tests.rs}`, all under 500, removing one entry from
`.file-length-baseline`. **Not** wired into the sdk prelude, the server, the
book or the examples — `cargo package` resolves `a2a-protocol-types` from
crates.io at `^0.9.0`, so that has to wait for the release (**B1**).

### 5. Four broken intra-doc links — `rate_limit/`

Q6. All four crates now emit zero rustdoc warnings at **both** default and
`--all-features`, per-crate as well as `--workspace`.

### 6. Two tests that asserted nothing

Q6. One deleted with a pointer to the tests that do cover it, one rewritten to
assert its own name.

### 7. Three `signing` feature descriptions that overstated

Q1. They now say they forward a primitive rather than enable an integration,
and name what the caller must do instead.

### 8. `push_delivery.rs` split rather than exempted

It crossed 500 lines when its new test landed. `mod.rs` 136, `tests.rs` 405.

---

## The backlog, prioritised and costed

Each item: the evidence that motivates it, the shape of the fix, and what would
prove it done.

### B1 — Re-export the agent-card constructors from `a2a_protocol_sdk::prelude`
* **Evidence:** Q3. 125 in-repo struct literals; 45% of the published
  hello-world is card boilerplate; the constructors exist but cannot be reached
  from the crate the README tells people to install.
* **Fix:** after `a2a-protocol-types` 0.10 is published — add `AgentCardBuilder`-
  adjacent names to `prelude`, rewrite the README Quick Start to a compiling
  snippet, migrate `examples/*/src/cards.rs` and the book's `first-agent.md`.
* **Cost:** hours, gated on a release. Blocked, not hard.
* **Done when:** the README Quick Start compiles as written, from an empty
  directory, with no struct literal.

### B2 — Run `hello-agent` and `deploy-agent` in CI
* **Evidence:** Q8. Executed by no workflow; `hello-agent` is the README's first
  example.
* **Fix:** two steps in `ci.yml`'s `example-surface` job, or a small
  `--self-check` mode in each so they exit rather than serve forever.
* **Cost:** an hour.
* **Done when:** `grep -rl -- '-p hello-agent' .github/workflows/` is non-empty
  and the step is registered in `prove_gates_fail.sh`.

### B3 — Make the 30-second push budget configurable
* **Evidence:** Q4/Q7. `Duration::from_secs(30)` hardcoded; it is the term that
  decides how many of an operator's webhooks are called.
* **Fix:** a `HandlerLimits` field. Adding a public field to a
  non-`#[non_exhaustive]` struct breaks struct-literal constructors, so it wants
  a release-planned batch and possibly `#[non_exhaustive]` first.
* **Cost:** an hour of code, a release decision.
* **Done when:** `with_push_delivery_budget` exists, and a test shows a raised
  budget reaches more configs.

### B4 — Wire agent-card signing into the server and client
* **Evidence:** Q1(1). Zero `#[cfg(feature = "signing")]` in either crate.
* **Fix:** server — sign the served card from a configured key at build time;
  client — an opt-in `with_card_trust_anchor(key)` that verifies on discovery
  and fails closed.
* **Cost:** a day, mostly API design for the trust-anchor shape.
* **Done when:** a test serves a signed card, a client configured with the key
  accepts it, and a client configured with a *different* key rejects it.

### B5 — A script for the `unwrap`/`expect` count
* **Evidence:** Q6. CONTRIBUTING's 26 is unreproducible by its own method; the
  naive method now reports 361 and the correct one 14.
* **Fix:** `scripts/check_unwrap_budget.sh`, ratchet-shaped like
  `check_file_lengths.sh`, excluding files reached via `#[cfg(test)] mod x;`.
* **Cost:** two hours including the gate-can-fail proof.
* **Done when:** it is in `ci.yml`, proven in `prove_gates_fail.sh`, and
  CONTRIBUTING quotes it rather than a typed number.

### B6 — Add `PostgresRateLimitCounter` to `codecov.yml`'s postgres exclusions
* **Evidence:** Q2.2. `rate_limit/shared.rs` is 0.00%, 60/60 missed, all three
  instrumented regions `#[cfg(feature = "postgres")]`; five equivalent store
  files are already excluded.
* **Fix:** move `PostgresRateLimitCounter` to `rate_limit/postgres_counter.rs`
  (so the `RateLimitCounter` trait's file stays measured) and add
  `**/rate_limit/postgres_counter.rs` to `ignore`.
* **Cost:** an hour. **Deliberately not done here:** the effect is computed
  server-side by Codecov and I cannot verify it from this session, and the change
  moves the public badge upward. An unverifiable change that flatters the number
  is the thing this document exists to catch.
* **Done when:** the next Codecov report shows `rate_limit` at ≳98% and the
  repository total moves by the amount the 60 lines account for, not more.

### B7 — `preflight.sh` should check free disk before it starts
* **Evidence:** Q3. It exhausted a 30 GB allowance mid-run; `target/` reached
  29 GB; its header warns qualitatively and gives no number.
* **Fix:** a `df` precheck against a measured figure, `--force` to override.
* **Cost:** thirty minutes.
* **Done when:** running it with a full disk prints the requirement and exits
  before compiling anything.

### B8 — `hot_reload.rs:255`
* **Evidence:** Q2.8. `signal(SignalKind::hangup()).expect(…)` inside a
  `tokio::spawn`ed task; failure kills hot reload and surfaces only through a
  `JoinHandle` the caller must poll.
* **Fix:** return `Result` from `spawn_signal_watcher`, or log-and-return.
* **Cost:** thirty minutes.

### B9 — Re-measure §3's SQLite streaming figures
* **Evidence:** Q4. 170–540 µs/event and 39× are the largest performance claims
  in this document and are UNVERIFIED at `4ddb7ab`.
* **Fix:** re-run the §3 probe; record the commit.
* **Cost:** an hour.

### B10 — Four chapters: troubleshooting, observability, multi-tenancy, security
* **Evidence:** Q9. 45 chapters, none of them the page an operator opens at 3am.
  Troubleshooting has no home at all (zero matching files); the other three have
  material scattered across 19, 7 and 16 files.
* **Fix:** four chapters. Troubleshooting first — it is the only one with nothing
  to lift from. It should name the two new signals this branch adds
  (`push_delivery{outcome="skipped"}`, the queue-full error) and say that
  `tracing` and `otel` are off by default.
* **Cost:** two to three days of writing.
* **Done when:** `SUMMARY.md` matches all four terms and
  `check_book_code.sh` still passes.

### B11 — A SLIMRPC example, and a Rust worker for `multi-lang-team`
* **Evidence:** Q8/Q10. No example uses SLIMRPC; `multi-lang-team` demonstrates
  Python, JS, Go and Java and not Rust (MEASURED, its README §1 starts four
  workers, none of them Rust).
* **Cost:** a day each.

### B12 — Bring the SLIMRPC binding under coverage, and drop `--no-verify`
* **Evidence:** Q5/Q10. 3,346 lines with no coverage measurement and no recorded
  exclusion; `cargo package --no-verify` does not build the tarball.
* **Cost:** half a day. **This is the gate on publishing** (Q10).

### B13 — A checker for gate *reachability*, not just gate failability
* **Evidence:** Q6. Two gates found whose trigger does not cover the event that
  breaks them — `dco.yml` (`pull_request` only) and now
  `check_benchmark_prose.sh` (bypassed by `GITHUB_TOKEN` pushes). Both found by
  a human reading `on:` blocks.
* **Fix:** a script that reads every workflow's `on:` block and every `git push`
  in the repository's own workflows, and asserts each gate-bearing job is
  reachable from every event that can change its inputs.
* **Cost:** a day. Highest-leverage new gate available.
* **Done when:** it fails if `benchmarks.yml`'s new prose step is deleted, and
  fails if `ci.yml` loses its `push` trigger.

---

## What I did **not** verify

Inherited as UNVERIFIED, so the next session starts from known-unknowns rather
than assumptions. Each says why, because "why" is usually the cost of checking.

### Could not run here

* **cargo-mutants.** Installed it (`cargo install cargo-mutants --locked`,
  succeeded) specifically to measure what moving `mutants.toml` to
  `.cargo/mutants.toml` would change, then lost the target directory to the
  disk exhaustion in Q3 and judged a fresh multi-hour mutation build a worse use
  of the remaining budget than the eight fixes. **So the root `mutants.toml` is
  still 12,669 bytes of configuration that nothing reads**, exactly as
  CONTRIBUTING §370 and `mutants.yml` say. The cheap next step is
  `cargo mutants --list` with and without the file in `.cargo/`, diffed — that
  needs no test runs and answers the scope question in minutes.
* **The official A2A TCK.** Needs a live SUT on fixed ports and the upstream
  suite checked out (`--tck`). The score in `docs/official-tck-findings.md` §1
  is carried forward unchecked.
* **The live-PostgreSQL suite** (26 `--ignored` tests) and the **multi-replica
  suite** (6). No PostgreSQL server here.
* **SPIRE / SLIMRPC SPIFFE tests.** The binding's CI job installs SPIRE; I did
  not.
* **The cross-SDK interop legs** (`itk/`) — they need Python, Node, Go and Java
  toolchains and running agents.
* **`cargo llvm-cov`.** Coverage numbers in Q5 come from the Codecov API for the
  2026-08-18 report, not from a local run.

### Chose not to, and why

* **Re-measuring §3's SQLite streaming figures** (**B9**). A meaningful re-run
  needs a release build and a quiet machine; this machine ran a full preflight
  concurrently for most of the session.
* **Changing `codecov.yml`** (**B6**). Reasoned at length in Q2.2: I can measure
  the cause and not the effect, and the change flatters the badge.
* **Moving `agent-team` out of `examples/`** (Q8). A wide-blast-radius rename
  that no test can verify; it wants its own PR.
* **The reverse direction of `check_api_reference.py`** — public items *missing*
  from the API Quick Reference. The script's header argues explicitly against
  it and I did not second-guess that.

### Carried forward from earlier sessions, not re-checked

* The official Rust SDK's MSRV (1.85), its test-suite size, and every other
  figure in `docs/rust-sdk-assessment.md`, dated 27 July 2026.
* `docs/official-tck-findings.md`'s score, its §5 and §17 open items.
* The claim that `a2a-slimrpc` 0.2.4 does not send `a2a-version` either (§2.1).
  This repository now does; whether the official crate still does not, I did not
  re-check.
* SLIMRPC **backpressure** (Q10, delta item 1). Neither specified nor
  implemented, and — given Q2.1 — the first thing I would look at next.

### A caution about one thing I *did* measure

The download analysis rests on crates.io's aggregate counters, which do not
distinguish a human, a CI job and a mirror. I can show that types 0.5.0's curve
is not an adoption curve and that exactly one external crate depends on any of
these four. I cannot show *who* is downloading, and nobody can from this data.
Read it as "the 27,045 figure does not mean what it looks like", not as a
headcount.

---

## Where to pick up

In the order they would repay, superseding the list at the end of Addendum 5
(whose four items remain open and are folded in below):

1. **B13 — gate reachability.** Two instances is a class, and both were found by
   eye.
2. **B1 — release, then reach the constructors from the prelude.** Also unblocks
   the `TaskState::ALL` collapse Addendum 5 left open.
3. **B12 — coverage for the SLIMRPC binding.** The gate on publishing it.
4. **B10 — the troubleshooting chapter**, first of four.
5. **B2, B5, B7, B8** — each an hour or two, each closes a measured hole.
6. **`.cargo/mutants.toml`** — still unresolved, and now with a cheap first step
   (`--list`, diffed) that does not need a mutation run.
7. **B3, B4, B6, B9, B11** — real, none of them urgent.

